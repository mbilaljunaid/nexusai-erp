import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Factory } from "lucide-react"; export default function CMTFactoryManagement() { const { data: factories = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/fashion-factories"], queryFn: () => fetch("/api/fashion-factories").then(r => r.json()).catch(() => []), }); const qualified = factories.filter((f: any) => f.status === "qualified").length; const avgPassRate = factories.length > 0 ? (factories.reduce((sum: number, f: any) => sum + (parseFloat(f.passRate) || 0), 0) / factories.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Factory className="h-8 w-8" /> CMT & Contract Manufacturing </h1> <p className="text-muted-foreground mt-2">Factory master, capacity planning, MOQ, lead times, QC gates, SMETA/ISO certs, AQL sampling</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Factories</p><p className="text-2xl font-bold">{factories.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Qualified</p><p className="text-2xl font-bold text-green-600">{qualified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Pass Rate</p><p className="text-2xl font-bold text-blue-600">{avgPassRate}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Qualification %</p><p className="text-2xl font-bold">{factories.length > 0 ? ((qualified / factories.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Factories</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : factories.length === 0 ? <p className="text-muted-foreground text-center py-4">No factories</p> : factories.slice(0, 10).map((f: any) => ( <div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`factory-${f.id}`}> <div className="flex-1"><p className="font-semibold">{f.factoryId}</p><p className="text-xs text-muted-foreground">Lead time: {f.leadTime} days • Capacity: {f.capacity}/week</p></div> <Badge variant={f.status === "qualified" ? "default" : "secondary"} className="text-xs">{f.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
