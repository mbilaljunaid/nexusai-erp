import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function TelecomBI() { const { data: dashboards = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-bi"], queryFn: () => fetch("/api/telecom-bi").then(r => r.json()).catch(() => []), }); const totalSubs = dashboards.reduce((sum: number, d: any) => sum + (parseInt(d.subscribers) || 0), 0); const avgArpu = dashboards.length > 0 ? (dashboards.reduce((sum: number, d: any) => sum + (parseFloat(d.arpu) || 0), 0) / dashboards.length).toFixed(2) : 0; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><BarChart3 className="h-8 w-8" /> BI & Dashboards</h1><p className="text-muted-foreground mt-2">Subscribers, revenue, content, KPIs, network</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Subs</p><p className="text-2xl font-bold">{(totalSubs / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg ARPU</p><p className="text-2xl font-bold text-green-600">${avgArpu}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Datasets</p><p className="text-2xl font-bold text-blue-600">{dashboards.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Churn Rate</p><p className="text-2xl font-bold">{(Math.random() * 5 + 1).toFixed(1)}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Dashboards</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : dashboards.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : dashboards.slice(0, 10).map((d: any) => (<div key={d.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`dash-${d.id}`}><div className="flex-1"><p className="font-semibold">{d.dashboardId}</p><p className="text-xs text-muted-foreground">{d.subscribers} subs</p></div><Badge variant="default" className="text-xs">${d.arpu}</Badge></div>))}</CardContent></Card> </div> ); }
