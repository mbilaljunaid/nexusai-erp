import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { ClipboardList } from "lucide-react"; export default function Enrollment() { const { data: enrollments = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/ed-enrollment"], queryFn: () => fetch("/api/ed-enrollment").then(r => r.json()).catch(() => []), }); const active = enrollments.filter((e: any) => e.status === "active").length; const graduated = enrollments.filter((e: any) => e.status === "graduated").length; return ( <StandardPage
      title="Enrolnt & Admissions"
      description="Admission tracking, enrollment, course registration, prerequisites, multi-program, graduation"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Enrollments</p><p className="text-2xl font-bold">{enrollments.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Graduated</p><p className="text-2xl font-bold text-blue-600">{graduated}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Grad %</p><p className="text-2xl font-bold">{enrollments.length > 0 ? ((graduated / enrollments.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Enrollments</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : enrollments.length === 0 ? <p className="text-muted-foreground text-center py-4">No enrollments</p> : enrollments.slice(0, 10).map((e: any) => ( <div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`enr-${e.id}`}> <div className="flex-1"><p className="font-semibold">{e.enrollmentId}</p><p className="text-xs text-muted-foreground">{e.programId}</p></div> <Badge variant={e.status === "active" ? "default" : "secondary"} className="text-xs">{e.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
