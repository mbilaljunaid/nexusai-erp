import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Clock } from "lucide-react"; export default function LabTurnaround() { const { data: tat = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hc-tat"], queryFn: () => fetch("/api/hc-tat").then(r => r.json()).catch(() => []), }); const withinSla = tat.filter((t: any) => parseFloat(t.actualHours) <= parseFloat(t.slaHours)).length; const avgTat = tat.length > 0 ? (tat.reduce((sum: number, t: any) => sum + (parseFloat(t.actualHours) || 0), 0) / tat.length).toFixed(1) : 0; return ( <StandardPage
      title="Lab Taround Time (TAT)"
      description="Test turnaround, SLA compliance, quality metrics, bottleneck analysis, process optimization"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Tests</p><p className="text-2xl font-bold">{tat.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Within SLA</p><p className="text-2xl font-bold text-green-600">{withinSla}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg TAT (hrs)</p><p className="text-2xl font-bold text-blue-600">{avgTat}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SLA %</p><p className="text-2xl font-bold">{tat.length > 0 ? ((withinSla / tat.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">TAT Analysis</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : tat.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : tat.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`tat-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.testId}</p><p className="text-xs text-muted-foreground">SLA: {t.slaHours}h</p></div> <Badge variant={parseFloat(t.actualHours) <= parseFloat(t.slaHours) ? "default" : "destructive"} className="text-xs">{t.actualHours}h</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
