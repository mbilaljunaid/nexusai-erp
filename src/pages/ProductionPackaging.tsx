import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Zap } from "lucide-react"; export default function ProductionPackaging() { const { data: workorders = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-production"], queryFn: () => fetch("/api/cpg-production").then(r => r.json()).catch(() => []), }); const completed = workorders.filter((w: any) => w.status === "completed").length; const inProgress = workorders.filter((w: any) => w.status === "in-progress").length; return ( <StandardPage
      title="Produon & Packaging"
      description="Work orders, line scheduling, material kitting, in-process QC, batch tracking, expiry dates"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total WOs</p><p className="text-2xl font-bold">{workorders.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{inProgress}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{workorders.length > 0 ? ((completed / workorders.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Production Orders</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : workorders.length === 0 ? <p className="text-muted-foreground text-center py-4">No orders</p> : workorders.slice(0, 10).map((w: any) => ( <div key={w.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`wo-${w.id}`}> <div className="flex-1"><p className="font-semibold">{w.woId}</p><p className="text-xs text-muted-foreground">Qty: {w.quantity} • Line: {w.lineId}</p></div> <Badge variant={w.status === "completed" ? "default" : "secondary"} className="text-xs">{w.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
