import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Layers } from "lucide-react"; export default function BOMRouting() { const { data: boms = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/mfg-bom"], queryFn: () => fetch("/api/mfg-bom").then(r => r.json()).catch(() => []), }); const complete = boms.filter((b: any) => b.status === "complete").length; return ( <StandardPage
      title="BOM &uting"
      description="Bill of Materials, routing sequences, sub-assemblies, costing, scrap calculation"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">BOMs</p><p className="text-2xl font-bold">{boms.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Complete</p><p className="text-2xl font-bold text-green-600">{complete}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-yellow-600">{boms.filter((b: any) => b.status === "draft").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Complete %</p><p className="text-2xl font-bold">{boms.length > 0 ? ((complete / boms.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">BOMs</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : boms.length === 0 ? <p className="text-muted-foreground text-center py-4">No BOMs</p> : boms.slice(0, 10).map((b: any) => ( <div key={b.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`bom-${b.id}`}> <div className="flex-1"><p className="font-semibold">{b.bomId}</p><p className="text-xs text-muted-foreground">{b.productId}</p></div> <Badge variant={b.status === "complete" ? "default" : "secondary"} className="text-xs">{b.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
