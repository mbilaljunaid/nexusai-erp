import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Calendar } from "lucide-react"; export default function ReservationsBookings() { const { data: bookings = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hospitality-bookings"], queryFn: () => fetch("/api/hospitality-bookings").then(r => r.json()).catch(() => []), }); const confirmed = bookings.filter((b: any) => b.status === "confirmed").length; return ( <StandardPage
      title="Reservations & Bookings"
      description="Bookings, availability, groups, cancellations"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Bookings</p><p className="text-2xl font-bold">{bookings.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Confirmed</p><p className="text-2xl font-bold text-green-600">{confirmed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{bookings.filter((b: any) => b.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Confirmation %</p><p className="text-2xl font-bold">{bookings.length > 0 ? ((confirmed / bookings.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Bookings</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : bookings.length === 0 ? <p className="text-muted-foreground text-center py-4">No bookings</p> : bookings.slice(0, 10).map((b: any) => (<div key={b.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`book-${b.id}`}><div className="flex-1"><p className="font-semibold">{b.bookingId}</p><p className="text-xs text-muted-foreground">{b.nights} nights</p></div><Badge variant={b.status === "confirmed" ? "default" : "secondary"} className="text-xs">{b.status}</Badge></div>))}</CardContent></Card> </StandardPage>
  );
}
