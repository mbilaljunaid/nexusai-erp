import { useState } from "react"; import { useQuery, useMutation } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Badge } from "@/components/ui/badge"; import { Package, Plus, Trash2 } from "lucide-react"; import { queryClient } from "@/lib/queryClient"; import { useToast } from "@/hooks/use-toast"; export default function ProductMasterCPG() { const { toast } = useToast(); const [newProd, setNewProd] = useState({ sku: "", name: "", packSize: "", upc: "", status: "draft" }); const { data: products = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-products"], queryFn: () => fetch("/api/cpg-products").then(r => r.json()).catch(() => []), }); const createMutation = useMutation({ mutationFn: (data: any) => fetch("/api/cpg-products", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }).then(r => r.json()), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/cpg-products"] }); setNewProd({ sku: "", name: "", packSize: "", upc: "", status: "draft" }); toast({ title: "Product created" }); }, }); const deleteMutation = useMutation({ mutationFn: (id: string) => fetch(`/api/cpg-products/${id}`, { method: "DELETE" }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/cpg-products"] }); toast({ title: "Product deleted" }); }, }); const active = products.filter((p: any) => p.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Package className="h-8 w-8" /> Product Master (SKU, Pack Size, Variant) </h1> <p className="text-muted-foreground mt-2">SKU registry, UPC/EAN/GTIN, pack sizes, variants, shelf-life, storage conditions, regulatory flags</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total SKUs</p><p className="text-2xl font-bold">{products.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Draft</p><p className="text-2xl font-bold text-yellow-600">{products.length - active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Launch %</p><p className="text-2xl font-bold">{products.length > 0 ? ((active / products.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card data-testid="card-new-product"> <CardHeader><CardTitle className="text-base">Create Product</CardTitle></CardHeader> <CardContent className="space-y-3"> <div className="grid grid-cols-5 gap-2"> <Input placeholder="SKU" value={newProd.sku} onChange={(e) => setNewProd({ ...newProd, sku: e.target.value })} data-testid="input-sku" className="text-sm" /> <Input placeholder="Name" value={newProd.name} onChange={(e) => setNewProd({ ...newProd, name: e.target.value })} data-testid="input-name" className="text-sm" /> <Input placeholder="Pack Size" value={newProd.packSize} onChange={(e) => setNewProd({ ...newProd, packSize: e.target.value })} data-testid="input-size" className="text-sm" /> <Input placeholder="UPC" value={newProd.upc} onChange={(e) => setNewProd({ ...newProd, upc: e.target.value })} data-testid="input-upc" className="text-sm" /> <Button disabled={createMutation.isPending || !newProd.sku} size="sm" data-testid="button-create"> <Plus className="w-3 h-3" /> </Button> </div> </CardContent> </Card> <Card> <CardHeader><CardTitle className="text-base">Products</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : products.length === 0 ? <p className="text-muted-foreground text-center py-4">No products</p> : products.map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`product-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.sku} - {p.name}</p><p className="text-xs text-muted-foreground">UPC: {p.upc}</p></div> <div className="flex gap-2 items-center"> <Badge variant={p.status === "active" ? "default" : "secondary"} className="text-xs">{p.status}</Badge> <Button size="icon" variant="ghost" data-testid={`button-delete-${p.id}`} className="h-7 w-7"> <Trash2 className="w-3 h-3" /> </Button> </div> </div> ))} </CardContent> </Card> </div> ); }
