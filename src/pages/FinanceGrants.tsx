import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { DollarSign } from "lucide-react"; export default function FinanceGrants() { const { data: budgets = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/gov-finance"], queryFn: () => fetch("/api/gov-finance").then(r => r.json()).catch(() => []), }); const utilized = budgets.filter((b: any) => b.status === "utilized").length; const totalBudget = budgets.reduce((sum: number, b: any) => sum + (parseFloat(b.amount) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><DollarSign className="h-8 w-8" /> Finance & Grants</h1><p className="text-muted-foreground mt-2">Budget, grants, subsidies, disbursements</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Budgets</p><p className="text-2xl font-bold">{budgets.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilized</p><p className="text-2xl font-bold text-green-600">{utilized}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total</p><p className="text-2xl font-bold text-blue-600">${(totalBudget / 1000000).toFixed(1)}M</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilization %</p><p className="text-2xl font-bold">{budgets.length > 0 ? ((utilized / budgets.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Budgets</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : budgets.length === 0 ? <p className="text-muted-foreground text-center py-4">No budgets</p> : budgets.slice(0, 10).map((b: any) => (<div key={b.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`bud-${b.id}`}><div className="flex-1"><p className="font-semibold">{b.budgetId}</p><p className="text-xs text-muted-foreground">${b.amount}</p></div><Badge variant={b.status === "utilized" ? "default" : "secondary"} className="text-xs">{b.status}</Badge></div>))}</CardContent></Card> </div> ); }
