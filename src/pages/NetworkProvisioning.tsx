import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Network } from "lucide-react"; export default function NetworkProvisioning() { const { data: resources = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-provisioning"], queryFn: () => fetch("/api/telecom-provisioning").then(r => r.json()).catch(() => []), }); const active = resources.filter((r: any) => r.status === "active").length; return ( <StandardPage
      title="NetwoProvisioning & Service Activation"
      description="Inventory, provisioning workflows, scheduling, automation, exceptions"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resources</p><p className="text-2xl font-bold">{resources.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{resources.filter((r: any) => r.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilization %</p><p className="text-2xl font-bold">{resources.length > 0 ? ((active / resources.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Resources</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : resources.length === 0 ? <p className="text-muted-foreground text-center py-4">No resources</p> : resources.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`res-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.resourceId}</p><p className="text-xs text-muted-foreground">{r.deviceId}</p></div> <Badge variant={r.status === "active" ? "default" : "secondary"} className="text-xs">{r.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
