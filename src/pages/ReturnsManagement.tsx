import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { RotateCcw } from "lucide-react"; export default function ReturnsManagement() { const { data: returns = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-returns"], queryFn: () => fetch("/api/retail-returns").then(r => r.json()), }); const approved = returns.filter((r: any) => r.status === "approved").length; const totalAmount = returns.reduce((sum: number, r: any) => sum + (parseFloat(r.refundAmount) || 0), 0); return ( <StandardPage
      title="Retur& Reverse Logistics"
      description="Return processing, refund management, reverse logistics, quality inspection, restocking"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Returns</p><p className="text-2xl font-bold">{returns.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Refund</p><p className="text-2xl font-bold text-blue-600">${(totalAmount / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approval %</p><p className="text-2xl font-bold">{returns.length > 0 ? ((approved / returns.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Returns</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : returns.length === 0 ? <p className="text-muted-foreground text-center py-4">No returns</p> : returns.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`ret-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.returnId}</p><p className="text-xs text-muted-foreground">${r.refundAmount}</p></div> <Badge variant={r.status === "approved" ? "default" : "secondary"} className="text-xs">{r.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
