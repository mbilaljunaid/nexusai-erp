import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { TrendingUp } from "lucide-react"; export default function ForecastingDemandCPG() { const { data: forecasts = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-forecast"], queryFn: () => fetch("/api/cpg-forecast").then(r => r.json()).catch(() => []), }); const avgAccuracy = forecasts.length > 0 ? (forecasts.reduce((sum: number, f: any) => sum + (parseFloat(f.accuracy) || 0), 0) / forecasts.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <TrendingUp className="h-8 w-8" /> Forecasting & Demand Planning </h1> <p className="text-muted-foreground mt-2">SKU forecasts by channel, safety stock, reorder points, promotion impact, AI models, allocation rules</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Forecasts</p><p className="text-2xl font-bold">{forecasts.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Accuracy</p><p className="text-2xl font-bold text-green-600">{avgAccuracy}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Periods</p><p className="text-2xl font-bold">{new Set(forecasts.map((f: any) => f.period)).size}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Channels</p><p className="text-2xl font-bold">{new Set(forecasts.map((f: any) => f.channel)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Demand Forecasts</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : forecasts.length === 0 ? <p className="text-muted-foreground text-center py-4">No forecasts</p> : forecasts.slice(0, 10).map((f: any) => ( <div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`fc-${f.id}`}> <div className="flex-1"><p className="font-semibold">{f.sku}</p><p className="text-xs text-muted-foreground">Qty: {f.quantity} • Accuracy: {f.accuracy}%</p></div> <Badge variant={parseFloat(f.accuracy) > 80 ? "default" : "secondary"} className="text-xs">{f.channel}</Badge> </div> ))} </CardContent> </Card> </div> ); }
