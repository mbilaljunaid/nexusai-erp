import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Package } from "lucide-react"; export default function SupplyChainMfg() { const { data: supply = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/mfg-supply"], queryFn: () => fetch("/api/mfg-supply").then(r => r.json()).catch(() => []), }); const received = supply.filter((s: any) => s.status === "received").length; return ( <StandardPage
      title="Supplhain & Procurement"
      description="Suppliers, requisitions, POs, receiving, performance, contracts, SLA"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">POs</p><p className="text-2xl font-bold">{supply.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Received</p><p className="text-2xl font-bold text-green-600">{received}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{supply.filter((s: any) => s.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Receipt %</p><p className="text-2xl font-bold">{supply.length > 0 ? ((received / supply.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">POs</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : supply.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : supply.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`po-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.poId}</p><p className="text-xs text-muted-foreground">{s.supplierId}</p></div> <Badge variant={s.status === "received" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
