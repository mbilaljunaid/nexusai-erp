import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function CitizenCaseManagement() { const { data: cases = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/gov-citizens"], queryFn: () => fetch("/api/gov-citizens").then(r => r.json()), }); const resolved = cases.filter((c: any) => c.status === "resolved").length; return ( <StandardPage
      title="Citizen & Case Management"
      description="Profiles, applications, service requests, tracking"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Cases</p><p className="text-2xl font-bold">{cases.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolved</p><p className="text-2xl font-bold text-green-600">{resolved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{cases.filter((c: any) => c.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolution %</p><p className="text-2xl font-bold">{cases.length > 0 ? ((resolved / cases.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Cases</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <TableSkeleton rows={4} /> : cases.length === 0 ? <p className="text-muted-foreground text-center py-4">No cases</p> : cases.slice(0, 10).map((c: any) => (<div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`case-${c.id}`}><div className="flex-1"><p className="font-semibold">{c.caseId}</p><p className="text-xs text-muted-foreground">{c.citizenId}</p></div><Badge variant={c.status === "resolved" ? "default" : "secondary"} className="text-xs">{c.status}</Badge></div>))}</CardContent></Card> </StandardPage>
  );
}
