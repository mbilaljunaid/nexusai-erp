import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Briefcase } from "lucide-react"; export default function WholesaleB2B() { const { data: orders = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/fashion-wholesale"], queryFn: () => fetch("/api/fashion-wholesale").then(r => r.json()), }); const confirmed = orders.filter((o: any) => o.status === "confirmed").length; const totalValue = orders.reduce((sum: number, o: any) => sum + (parseFloat(o.value) || 0), 0); return ( <StandardPage
      title="Wholee, B2B Orders & Showroom"
      description="Line sheets, buyer portal, seasonal prebook, FOH/backorder, payment terms, showroom sampling, dealer allocation"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Orders</p><p className="text-2xl font-bold">{orders.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Confirmed</p><p className="text-2xl font-bold text-green-600">{confirmed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Value</p><p className="text-2xl font-bold">${(totalValue / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Buyers</p><p className="text-2xl font-bold">{new Set(orders.map((o: any) => o.buyerId)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Orders</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : orders.length === 0 ? <p className="text-muted-foreground text-center py-4">No orders</p> : orders.slice(0, 10).map((o: any) => ( <div key={o.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`order-${o.id}`}> <div className="flex-1"><p className="font-semibold">{o.poId}</p><p className="text-xs text-muted-foreground">Buyer: {o.buyerId}</p></div> <Badge variant={o.status === "confirmed" ? "default" : "secondary"} className="text-xs">${o.value}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
