import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Smartphone } from "lucide-react"; export default function DeviceSIMManagement() { const { data: devices = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-devices"], queryFn: () => fetch("/api/telecom-devices").then(r => r.json()), }); const active = devices.filter((d: any) => d.status === "active").length; const underWarranty = devices.filter((d: any) => d.underWarranty).length; return ( <StandardPage
      title="DevicSIM & Equipment Management"
      description="Device master, IMEI, SIM activation, device assignment, warranty tracking, stock management"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Devices</p><p className="text-2xl font-bold">{devices.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Under Warranty</p><p className="text-2xl font-bold text-blue-600">{underWarranty}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{devices.length > 0 ? ((active / devices.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Devices</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : devices.length === 0 ? <p className="text-muted-foreground text-center py-4">No devices</p> : devices.slice(0, 10).map((d: any) => ( <div key={d.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`device-${d.id}`}> <div className="flex-1"><p className="font-semibold">{d.deviceId}</p><p className="text-xs text-muted-foreground">IMEI: {d.imei}</p></div> <Badge variant={d.status === "active" ? "default" : "secondary"} className="text-xs">{d.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
