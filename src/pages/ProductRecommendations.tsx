import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Sparkles } from "lucide-react"; export default function ProductRecommendations() { const { data: recs = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-recommendations"], queryFn: () => fetch("/api/retail-recommendations").then(r => r.json()).catch(() => []), }); const highConfidence = recs.filter((r: any) => parseFloat(r.confidence) > 80).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Sparkles className="h-8 w-8" /> AI & Personalization </h1> <p className="text-muted-foreground mt-2">Product recommendations, demand forecasting, churn prediction, dynamic pricing suggestions</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recommendations</p><p className="text-2xl font-bold">{recs.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">High Confidence</p><p className="text-2xl font-bold text-green-600">{highConfidence}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Confidence</p><p className="text-2xl font-bold text-blue-600">{recs.length > 0 ? (recs.reduce((sum: number, r: any) => sum + (parseFloat(r.confidence) || 0), 0) / recs.length).toFixed(0) : 0}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Quality %</p><p className="text-2xl font-bold">{recs.length > 0 ? ((highConfidence / recs.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Recommendations</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : recs.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : recs.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`rec-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.productId}</p><p className="text-xs text-muted-foreground">Score: {r.confidence}%</p></div> <Badge variant={parseFloat(r.confidence) > 80 ? "default" : "secondary"} className="text-xs">{r.confidence}%</Badge> </div> ))} </CardContent> </Card> </div> ); }
