import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { RefreshCw } from "lucide-react"; export default function ReturnsExchanges() { const { data: rmas = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/fashion-returns"], queryFn: () => fetch("/api/fashion-returns").then(r => r.json()).catch(() => []), }); const resellable = rmas.filter((r: any) => r.disposition === "resell").length; const avgReturnRate = rmas.length > 0 ? (rmas.reduce((sum: number, r: any) => sum + (parseFloat(r.returnRate) || 0), 0) / rmas.length).toFixed(1) : 0; return ( <StandardPage
      title="Retur Fit & Exchange Management"
      description="RMA, fit feedback, size-swap, QC disposition, return % by SKU/size, analytics & trend detection"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">RMAs</p><p className="text-2xl font-bold">{rmas.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resellable</p><p className="text-2xl font-bold text-green-600">{resellable}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Return Rate</p><p className="text-2xl font-bold text-orange-600">{avgReturnRate}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Reasons Tracked</p><p className="text-2xl font-bold">{new Set(rmas.map((r: any) => r.reason)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Returns</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : rmas.length === 0 ? <p className="text-muted-foreground text-center py-4">No returns</p> : rmas.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`rma-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.rmaId}</p><p className="text-xs text-muted-foreground">Reason: {r.reason}</p></div> <Badge variant={r.disposition === "resell" ? "default" : "secondary"} className="text-xs">{r.disposition}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
