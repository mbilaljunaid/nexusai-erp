import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Beaker } from "lucide-react"; export default function RecipeBOMMaster() { const { data: recipes = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-recipes"], queryFn: () => fetch("/api/cpg-recipes").then(r => r.json()).catch(() => []), }); const approved = recipes.filter((r: any) => r.status === "approved").length; return (<StandardPage
      title="Recip BOM Master"
      description="Formulation percentages, process steps, batch sizes, QC parameters, versioning & revision control"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recipes</p><p className="text-2xl font-bold">{recipes.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Draft</p><p className="text-2xl font-bold text-yellow-600">{recipes.length - approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approval %</p><p className="text-2xl font-bold">{recipes.length > 0 ? ((approved / recipes.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Recipes</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <TableSkeleton rows={4} /> : recipes.length === 0 ? <p className="text-muted-foreground text-center py-4">No recipes</p> : recipes.slice(0, 10).map((r: any) => (<div key={r.id} className="p-2 border rounded text-sm hover-elevate" data-testid={`recipe-${r.id}`}> <p className="font-semibold">{r.recipeId}</p> <p className="text-xs text-muted-foreground">SKU: {r.sku} • v{r.version}</p> </div>))} </CardContent> </Card> </StandardPage>
  );
}
