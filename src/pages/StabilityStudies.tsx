import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Thermometer } from "lucide-react"; export default function StabilityStudies() { const { data: studies = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/pharma-stability"], queryFn: () => fetch("/api/pharma-stability").then(r => r.json()), }); const completed = studies.filter((s: any) => s.status === "completed").length; const inProgress = studies.filter((s: any) => s.status === "in-progress").length; const avgShelfLife = studies.length > 0 ? (studies.reduce((sum: number, s: any) => sum + (parseInt(s.shelfLife) || 0), 0) / studies.length).toFixed(0) : 0; return ( <StandardPage
      title="Stabiy Studies & Shelf-Life Management"
      description="Accelerated & ambient studies, ICH Q1A/Q1B, timepoint data, shelf-life calculation & extrapolation"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Studies</p><p className="text-2xl font-bold">{studies.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{inProgress}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Shelf-Life</p><p className="text-2xl font-bold">{avgShelfLife} mo</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Studies</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : studies.length === 0 ? <p className="text-muted-foreground text-center py-4">No studies</p> : studies.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`study-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.studyId}</p><p className="text-xs text-muted-foreground">Condition: {s.condition} • Shelf-Life: {s.shelfLife} months</p></div> <Badge variant={s.status === "completed" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
