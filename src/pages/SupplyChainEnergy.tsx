import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Package } from "lucide-react"; export default function SupplyChainEnergy() { const { data: suppliers = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/energy-supply"], queryFn: () => fetch("/api/energy-supply").then(r => r.json()).catch(() => []), }); const active = suppliers.filter((s: any) => s.status === "active").length; return ( <StandardPage
      title="Supply Chain & Procurement"
      description="Vendors, requisitions, orders, contracts, SLA tracking"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Suppliers</p><p className="text-2xl font-bold">{suppliers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{suppliers.filter((s: any) => s.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{suppliers.length > 0 ? ((active / suppliers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Suppliers</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : suppliers.length === 0 ? <p className="text-muted-foreground text-center py-4">No suppliers</p> : suppliers.slice(0, 10).map((s: any) => (<div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`supp-${s.id}`}><div className="flex-1"><p className="font-semibold">{s.supplierId}</p><p className="text-xs text-muted-foreground">{s.region}</p></div><Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge></div>))}</CardContent></Card> </StandardPage>
  );
}
