import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Beaker } from "lucide-react"; export default function FormulationComposer() { const { data: formulations = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/pharma-formulations"], queryFn: () => fetch("/api/pharma-formulations").then(r => r.json()), }); const approved = formulations.filter((f: any) => f.status === "approved").length; const totalIngredients = formulations.reduce((sum: number, f: any) => sum + (parseInt(f.ingredientCount) || 0), 0); return (<StandardPage
      title="Formuion Composer"
      description="eBOM/mBOM, excipient ratios, calculation engine, versioning, impact analysis, tech transfer"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Formulations</p><p className="text-2xl font-bold">{formulations.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Versions</p><p className="text-2xl font-bold">{formulations.length > 0 ? formulations.length : 0}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Ingredients</p><p className="text-2xl font-bold">{totalIngredients}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Formulations</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : formulations.length === 0 ? <p className="text-muted-foreground text-center py-4">No formulations</p> : formulations.slice(0, 10).map((f: any) => (<div key={f.id} className="p-2 border rounded text-sm hover-elevate" data-testid={`formulation-${f.id}`}> <p className="font-semibold">{f.formulationId} v{f.version}</p> <p className="text-xs text-muted-foreground">Product: {f.productId} • Ingredients: {f.ingredientCount}</p> </div>))} </CardContent> </Card> </StandardPage>
  );
}
