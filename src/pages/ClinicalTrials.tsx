import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Activity } from "lucide-react"; export default function ClinicalTrials() { const { data: trials = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hc-trials"], queryFn: () => fetch("/api/hc-trials").then(r => r.json()).catch(() => []), }); const active = trials.filter((t: any) => t.status === "active").length; const totalEnrolled = trials.reduce((sum: number, t: any) => sum + (parseInt(t.enrolled) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Activity className="h-8 w-8" /> Clinical Trials & R&D </h1> <p className="text-muted-foreground mt-2">Trial protocols, phases, sites, participant enrollment, randomization, adverse events, lab integration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Trials</p><p className="text-2xl font-bold">{trials.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Enrolled</p><p className="text-2xl font-bold text-blue-600">{totalEnrolled}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{trials.length > 0 ? ((active / trials.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Clinical Trials</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : trials.length === 0 ? <p className="text-muted-foreground text-center py-4">No trials</p> : trials.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`trial-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.trialId}</p><p className="text-xs text-muted-foreground">Enrolled: {t.enrolled}</p></div> <Badge variant={t.status === "active" ? "default" : "secondary"} className="text-xs">{t.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
