import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CreditCard } from "lucide-react"; export default function FashionPOS() { const { data: transactions = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/fashion-pos"], queryFn: () => fetch("/api/fashion-pos").then(r => r.json()).catch(() => []), }); const totalSales = transactions.reduce((sum: number, t: any) => sum + (parseFloat(t.amount) || 0), 0); const exchangeReturns = transactions.filter((t: any) => t.type === "return" || t.type === "exchange").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CreditCard className="h-8 w-8" /> POS & In-Store Experience </h1> <p className="text-muted-foreground mt-2">Size/color selector, endless aisle, reserve & try-on, returns/exchanges by size, planogram integration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Transactions</p><p className="text-2xl font-bold">{transactions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Sales</p><p className="text-2xl font-bold text-green-600">${totalSales.toFixed(0)}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Returns/Exchanges</p><p className="text-2xl font-bold text-orange-600">{exchangeReturns}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Stores</p><p className="text-2xl font-bold">{new Set(transactions.map((t: any) => t.storeId)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Transactions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : transactions.length === 0 ? <p className="text-muted-foreground text-center py-4">No transactions</p> : transactions.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`trans-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.transactionId}</p><p className="text-xs text-muted-foreground">{t.type}</p></div> <Badge variant="default" className="text-xs">${t.amount}</Badge> </div> ))} </CardContent> </Card> </div> ); }
