import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; import { useEnterpriseStore } from "@/lib/enterpriseStore"; export default function SubscriberAccounts() { const { businessUnitId } = useEnterpriseStore(); const { data: subscribers = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-subscribers", businessUnitId], queryFn: () => fetch("/api/telecom-subscribers", { headers: businessUnitId ? { "x-business-unit-id": businessUnitId } : undefined }).then(r => r.json()).catch(() => []), }); const active = subscribers.filter((s: any) => s.status === "active").length; return (<StandardPage
      title="Subscriber & Account Management"
      description="Profiles, plans, subscriptions, usage tracking"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Subscribers</p><p className="text-2xl font-bold">{subscribers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{subscribers.filter((s: any) => s.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{subscribers.length > 0 ? ((active / subscribers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Subscribers</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : subscribers.length === 0 ? <p className="text-muted-foreground text-center py-4">No subscribers</p> : subscribers.slice(0, 10).map((s: any) => (<div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`sub-${s.id}`}><div className="flex-1"><p className="font-semibold">{s.subscriberId}</p><p className="text-xs text-muted-foreground">{s.plan}</p></div><Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge></div>))}</CardContent></Card> </StandardPage>
  );
}
