import { useQuery } from "@tanstack/react-query";
import { TableSkeleton } from "@/components/shared/TableSkeleton";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Tag } from "lucide-react"; export default function PricingPromotionsRetail() { const { data: promotions = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-pricing"], queryFn: () => fetch("/api/retail-pricing").then(r => r.json()), }); const active = promotions.filter((p: any) => p.status === "active").length; return ( <StandardPage
      title="Pricing & Promotions"
      description="Price lists, discounts, bundles, campaigns"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Promotions</p><p className="text-2xl font-bold">{promotions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{promotions.filter((p: any) => p.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{promotions.length > 0 ? ((active / promotions.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Promotions</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <TableSkeleton rows={4} /> : promotions.length === 0 ? <p className="text-muted-foreground text-center py-4">No promotions</p> : promotions.slice(0, 10).map((p: any) => (<div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`promo-${p.id}`}><div className="flex-1"><p className="font-semibold">{p.name}</p><p className="text-xs text-muted-foreground">{p.discount}% off</p></div><Badge variant={p.status === "active" ? "default" : "secondary"} className="text-xs">{p.status}</Badge></div>))}</CardContent></Card> </StandardPage>
  );
}
