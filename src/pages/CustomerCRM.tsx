import { useQuery } from "@tanstack/react-query";
import { StandardPage } from "@/components/layout/StandardPage"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function CustomerCRM() { const { data: customers = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-customers"], queryFn: () => fetch("/api/retail-customers").then(r => r.json()).catch(() => []), }); const vip = customers.filter((c: any) => c.tier === "vip").length; const totalPoints = customers.reduce((sum: number, c: any) => sum + (parseInt(c.loyaltyPoints) || 0), 0); return ( <StandardPage
      title="Custo Management & CRM"
      description="Master data, segmentation, interaction history, loyalty tracking, targeting, rewards"
    > <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Customers</p><p className="text-2xl font-bold">{customers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">VIP Tier</p><p className="text-2xl font-bold text-green-600">{vip}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Points</p><p className="text-2xl font-bold text-blue-600">{(totalPoints / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">VIP %</p><p className="text-2xl font-bold">{customers.length > 0 ? ((vip / customers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Customers</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : customers.length === 0 ? <p className="text-muted-foreground text-center py-4">No customers</p> : customers.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`cust-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.customerId}</p><p className="text-xs text-muted-foreground">{c.email}</p></div> <Badge variant={c.tier === "vip" ? "default" : "secondary"} className="text-xs">{c.tier}</Badge> </div> ))} </CardContent> </Card> </StandardPage>
  );
}
