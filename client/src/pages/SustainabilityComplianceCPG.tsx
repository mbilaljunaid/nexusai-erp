import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Leaf } from "lucide-react"; export default function SustainabilityComplianceCPG() { const { data: compliance = [], isLoading } = useQuery({ queryKey: ["/api/cpg-compliance"],  }); const certified = compliance.filter((c: any) => c.certified).length; const compliant = compliance.filter((c: any) => c.compliant).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Leaf className="h-8 w-8" /> Sustainability & Compliance </h1> <p className="text-muted-foreground mt-2">Ingredient certs, packaging labels, eco-scores, RSL compliance, regulatory flagging, recyclability</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Items Tracked</p><p className="text-2xl font-bold">{compliance.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Certified</p><p className="text-2xl font-bold text-green-600">{certified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliant</p><p className="text-2xl font-bold text-blue-600">{compliant}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliance %</p><p className="text-2xl font-bold">{compliance.length > 0 ? ((compliant / compliance.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Compliance Items</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : compliance.length === 0 ? <p className="text-muted-foreground text-center py-4">No items</p> : compliance.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`compliance-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.itemId}</p><p className="text-xs text-muted-foreground">Eco Score: {c.ecoScore}</p></div> <Badge variant={c.compliant ? "default" : "destructive"} className="text-xs">{c.type}</Badge> </div> ))} </CardContent> </Card> </div> ); }
