import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Shield } from "lucide-react"; export default function ComplianceAudit() { const { data: compliance = [], isLoading } = useQuery({ queryKey: ["/api/telecom-compliance"],  }); const compliant = compliance.filter((c: any) => c.status === "compliant").length; const kycDone = compliance.filter((c: any) => c.kycStatus === "verified").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Shield className="h-8 w-8" /> Compliance & Regulatory Management </h1> <p className="text-muted-foreground mt-2">KYC verification, licensing, GDPR compliance, data privacy, anti-fraud, regulatory audit trails</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Records</p><p className="text-2xl font-bold">{compliance.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">KYC Verified</p><p className="text-2xl font-bold text-green-600">{kycDone}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliant</p><p className="text-2xl font-bold text-blue-600">{compliant}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliance %</p><p className="text-2xl font-bold">{compliance.length > 0 ? ((compliant / compliance.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Compliance Records</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : compliance.length === 0 ? <p className="text-muted-foreground text-center py-4">No records</p> : compliance.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`comp-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.customerId}</p><p className="text-xs text-muted-foreground">KYC: {c.kycStatus}</p></div> <Badge variant={c.status === "compliant" ? "default" : "secondary"} className="text-xs">{c.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
