import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CheckCircle } from "lucide-react"; export default function QualityCompliance() { const { data: inspections = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/mfg-quality"], queryFn: () => fetch("/api/mfg-quality").then(r => r.json()).catch(() => []), }); const passed = inspections.filter((i: any) => i.status === "passed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CheckCircle className="h-8 w-8" /> Quality & Compliance </h1> <p className="text-muted-foreground mt-2">Inspection, test plans, deviations, CAPA, ISO/IATF standards, audit logs</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inspections</p><p className="text-2xl font-bold">{inspections.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Passed</p><p className="text-2xl font-bold text-green-600">{passed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Failed</p><p className="text-2xl font-bold text-red-600">{inspections.filter((i: any) => i.status === "failed").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pass Rate</p><p className="text-2xl font-bold">{inspections.length > 0 ? ((passed / inspections.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Inspections</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : inspections.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : inspections.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`insp-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.inspectionId}</p><p className="text-xs text-muted-foreground">{i.productId}</p></div> <Badge variant={i.status === "passed" ? "default" : "destructive"} className="text-xs">{i.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
