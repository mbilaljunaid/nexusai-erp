import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { ClipboardList } from "lucide-react"; export default function Enrollment() { const { data: enrollments = [], isLoading } = useQuery({ queryKey: ["/api/ed-enrollment"],  }); const active = enrollments.filter((e: any) => e.status === "active").length; const graduated = enrollments.filter((e: any) => e.status === "graduated").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <ClipboardList className="h-8 w-8" /> Enrollment & Admissions </h1> <p className="text-muted-foreground mt-2">Admission tracking, enrollment, course registration, prerequisites, multi-program, graduation</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Enrollments</p><p className="text-2xl font-bold">{enrollments.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Graduated</p><p className="text-2xl font-bold text-blue-600">{graduated}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Grad %</p><p className="text-2xl font-bold">{enrollments.length > 0 ? ((graduated / enrollments.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Enrollments</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : enrollments.length === 0 ? <p className="text-muted-foreground text-center py-4">No enrollments</p> : enrollments.slice(0, 10).map((e: any) => ( <div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`enr-${e.id}`}> <div className="flex-1"><p className="font-semibold">{e.enrollmentId}</p><p className="text-xs text-muted-foreground">{e.programId}</p></div> <Badge variant={e.status === "active" ? "default" : "secondary"} className="text-xs">{e.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
