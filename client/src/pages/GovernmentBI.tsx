import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function GovernmentBI() { const { data: kpis = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/gov-bi"], queryFn: () => fetch("/api/gov-bi").then(r => r.json()).catch(() => []), }); const onTrack = kpis.filter((k: any) => k.status === "on-track").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><BarChart3 className="h-8 w-8" /> Government BI & Dashboards</h1><p className="text-muted-foreground mt-2">Services, projects, finance, HR KPIs</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">KPIs</p><p className="text-2xl font-bold">{kpis.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">On Track</p><p className="text-2xl font-bold text-green-600">{onTrack}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">At Risk</p><p className="text-2xl font-bold text-yellow-600">{kpis.filter((k: any) => k.status === "at-risk").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Performance %</p><p className="text-2xl font-bold">{kpis.length > 0 ? ((onTrack / kpis.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">KPIs</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : kpis.length === 0 ? <p className="text-muted-foreground text-center py-4">No KPIs</p> : kpis.slice(0, 10).map((k: any) => (<div key={k.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`kpi-${k.id}`}><div className="flex-1"><p className="font-semibold">{k.kpiId}</p><p className="text-xs text-muted-foreground">{k.departmentId}</p></div><Badge variant={k.status === "on-track" ? "default" : "secondary"} className="text-xs">{k.status}</Badge></div>))}</CardContent></Card> </div> ); }
