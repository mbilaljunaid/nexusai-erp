import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Sparkles } from "lucide-react"; export default function NetworkOptimization() { const { data: predictions = [], isLoading } = useQuery({ queryKey: ["/api/telecom-optimization"], queryFn: () => fetch("/api/telecom-optimization").then(r => r.json()).catch(() => []), }); const faultAlerts = predictions.filter((p: any) => p.type === "fault").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Sparkles className="h-8 w-8" /> AI & Network Optimization </h1> <p className="text-muted-foreground mt-2">Predictive faults, traffic optimization, churn prediction, resource allocation</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Models</p><p className="text-2xl font-bold">{predictions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Fault Alerts</p><p className="text-2xl font-bold text-red-600">{faultAlerts}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Accuracy</p><p className="text-2xl font-bold text-blue-600">92%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Optimized</p><p className="text-2xl font-bold text-green-600">{predictions.filter((p: any) => p.status === "optimized").length}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Predictions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : predictions.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : predictions.slice(0, 10).map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`pred-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.predictionId}</p><p className="text-xs text-muted-foreground">{p.type}</p></div> <Badge variant={p.status === "optimized" ? "default" : "secondary"} className="text-xs">{p.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
