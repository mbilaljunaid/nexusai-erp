import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Smartphone } from "lucide-react"; export default function DeviceSIMManagement() { const { data: devices = [], isLoading } = useQuery({ queryKey: ["/api/telecom-devices"],  }); const active = devices.filter((d: any) => d.status === "active").length; const underWarranty = devices.filter((d: any) => d.underWarranty).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Smartphone className="h-8 w-8" /> Device, SIM & Equipment Management </h1> <p className="text-muted-foreground mt-2">Device master, IMEI, SIM activation, device assignment, warranty tracking, stock management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Devices</p><p className="text-2xl font-bold">{devices.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Under Warranty</p><p className="text-2xl font-bold text-blue-600">{underWarranty}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{devices.length > 0 ? ((active / devices.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Devices</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : devices.length === 0 ? <p className="text-muted-foreground text-center py-4">No devices</p> : devices.slice(0, 10).map((d: any) => ( <div key={d.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`device-${d.id}`}> <div className="flex-1"><p className="font-semibold">{d.deviceId}</p><p className="text-xs text-muted-foreground">IMEI: {d.imei}</p></div> <Badge variant={d.status === "active" ? "default" : "secondary"} className="text-xs">{d.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
