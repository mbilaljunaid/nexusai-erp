import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BookOpen } from "lucide-react"; export default function ELNResearchNotebook() { const { data: notebooks = [], isLoading } = useQuery({ queryKey: ["/api/pharma-eln"],  }); const completed = notebooks.filter((n: any) => n.status === "completed").length; const draftEntries = notebooks.filter((n: any) => n.status === "draft").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BookOpen className="h-8 w-8" /> ELN - Research Notebook </h1> <p className="text-muted-foreground mt-2">Structured observations, attachments, versioning, electronic signatures, 21 CFR Part 11 compliant</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Entries</p><p className="text-2xl font-bold">{notebooks.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Draft</p><p className="text-2xl font-bold text-yellow-600">{draftEntries}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Signature %</p><p className="text-2xl font-bold">{notebooks.length > 0 ? ((completed / notebooks.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Research Entries</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : notebooks.length === 0 ? <p className="text-muted-foreground text-center py-4">No entries</p> : notebooks.slice(0, 10).map((n: any) => ( <div key={n.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`entry-${n.id}`}> <div className="flex-1"><p className="font-semibold">{n.studyId}</p><p className="text-xs text-muted-foreground">Researcher: {n.researcher}</p></div> <Badge variant={n.status === "completed" ? "default" : "secondary"} className="text-xs">{n.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
