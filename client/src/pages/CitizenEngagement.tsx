import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { MessageSquare } from "lucide-react"; export default function CitizenEngagement() { const { data: interactions = [], isLoading } = useQuery({ queryKey: ["/api/gov-engagement"],  }); const resolved = interactions.filter((i: any) => i.status === "resolved").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><MessageSquare className="h-8 w-8" /> Citizen Engagement & CRM</h1><p className="text-muted-foreground mt-2">Communications, feedback, complaints, satisfaction</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Interactions</p><p className="text-2xl font-bold">{interactions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolved</p><p className="text-2xl font-bold text-green-600">{resolved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{interactions.filter((i: any) => i.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolution %</p><p className="text-2xl font-bold">{interactions.length > 0 ? ((resolved / interactions.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Interactions</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : interactions.length === 0 ? <p className="text-muted-foreground text-center py-4">No interactions</p> : interactions.slice(0, 10).map((i: any) => (<div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`int-${i.id}`}><div className="flex-1"><p className="font-semibold">{i.interactionId}</p><p className="text-xs text-muted-foreground">{i.type}</p></div><Badge variant={i.status === "resolved" ? "default" : "secondary"} className="text-xs">{i.status}</Badge></div>))}</CardContent></Card> </div> ); }
