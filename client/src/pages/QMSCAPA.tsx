import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { AlertCircle } from "lucide-react"; export default function QMSCAPA() { const { data: issues = [], isLoading } = useQuery({ queryKey: ["/api/pharma-qms"],  }); const capas = issues.filter((i: any) => i.type === "CAPA").length; const ncrs = issues.filter((i: any) => i.type === "NCR").length; const closed = issues.filter((i: any) => i.status === "closed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <AlertCircle className="h-8 w-8" /> QMS - CAPA, NCR & Change Control </h1> <p className="text-muted-foreground mt-2">CAPA (corrective/preventive), NCR, complaints, ECO/ECR, root cause analysis, effectiveness checks</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Issues</p><p className="text-2xl font-bold">{issues.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">CAPAs</p><p className="text-2xl font-bold text-orange-600">{capas}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">NCRs</p><p className="text-2xl font-bold text-yellow-600">{ncrs}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Closed</p><p className="text-2xl font-bold text-green-600">{closed}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Quality Issues</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : issues.length === 0 ? <p className="text-muted-foreground text-center py-4">No issues</p> : issues.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`issue-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.issueId}</p><p className="text-xs text-muted-foreground">Type: {i.type} • Due: {i.dueDate}</p></div> <Badge variant={i.status === "closed" ? "default" : "secondary"} className="text-xs">{i.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
