import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { MessageSquare } from "lucide-react"; export default function TelecomCRM() { const { data: tickets = [], isLoading } = useQuery({ queryKey: ["/api/telecom-crm"], queryFn: () => fetch("/api/telecom-crm").then(r => r.json()).catch(() => []), }); const resolved = tickets.filter((t: any) => t.status === "resolved").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><MessageSquare className="h-8 w-8" /> CRM & Support</h1><p className="text-muted-foreground mt-2">Tickets, complaints, escalation, SLA tracking</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Tickets</p><p className="text-2xl font-bold">{tickets.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolved</p><p className="text-2xl font-bold text-green-600">{resolved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Open</p><p className="text-2xl font-bold text-yellow-600">{tickets.filter((t: any) => t.status === "open").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolution %</p><p className="text-2xl font-bold">{tickets.length > 0 ? ((resolved / tickets.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Tickets</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : tickets.length === 0 ? <p className="text-muted-foreground text-center py-4">No tickets</p> : tickets.slice(0, 10).map((t: any) => (<div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`tkt-${t.id}`}><div className="flex-1"><p className="font-semibold">{t.ticketId}</p><p className="text-xs text-muted-foreground">Priority: {t.priority}</p></div><Badge variant={t.status === "resolved" ? "default" : "secondary"} className="text-xs">{t.status}</Badge></div>))}</CardContent></Card> </div> ); }
