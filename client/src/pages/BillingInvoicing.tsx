import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Receipt } from "lucide-react"; export default function BillingInvoicing() { const { data: invoices = [], isLoading } = useQuery({ queryKey: ["/api/telecom-billing"],  }); const paid = invoices.filter((i: any) => i.status === "paid").length; const totalValue = invoices.reduce((sum: number, i: any) => sum + (parseFloat(i.amount) || 0), 0); const outstanding = invoices.filter((i: any) => i.status === "outstanding").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Receipt className="h-8 w-8" /> Billing, Invoicing & Collections </h1> <p className="text-muted-foreground mt-2">Billing cycles, rate cards, invoices, payment capture, taxes, collections, arrears management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Invoices</p><p className="text-2xl font-bold">{invoices.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Value</p><p className="text-2xl font-bold text-green-600">${(totalValue / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Outstanding</p><p className="text-2xl font-bold text-red-600">{outstanding}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Collection %</p><p className="text-2xl font-bold text-blue-600">{invoices.length > 0 ? ((paid / invoices.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Invoices</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : invoices.length === 0 ? <p className="text-muted-foreground text-center py-4">No invoices</p> : invoices.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`inv-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.invoiceId}</p><p className="text-xs text-muted-foreground">{i.customerId}</p></div> <Badge variant={i.status === "paid" ? "default" : "secondary"} className="text-xs">${i.amount}</Badge> </div> ))} </CardContent> </Card> </div> ); }
