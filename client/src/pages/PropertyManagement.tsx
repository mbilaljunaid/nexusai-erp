import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Building2 } from "lucide-react"; export default function PropertyManagement() { const { data: properties = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hospitality-properties"], queryFn: () => fetch("/api/hospitality-properties").then(r => r.json()).catch(() => []), }); const active = properties.filter((p: any) => p.status === "active").length; const totalRooms = properties.reduce((sum: number, p: any) => sum + (parseInt(p.rooms) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Building2 className="h-8 w-8" /> Property & Room Management</h1><p className="text-muted-foreground mt-2">Properties, rooms, amenities, maintenance, occupancy</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Properties</p><p className="text-2xl font-bold">{properties.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Rooms</p><p className="text-2xl font-bold text-blue-600">{totalRooms}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{properties.length > 0 ? ((active / properties.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Properties</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : properties.length === 0 ? <p className="text-muted-foreground text-center py-4">No properties</p> : properties.slice(0, 10).map((p: any) => (<div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`prop-${p.id}`}><div className="flex-1"><p className="font-semibold">{p.propertyId}</p><p className="text-xs text-muted-foreground">{p.rooms} rooms</p></div><Badge variant={p.status === "active" ? "default" : "secondary"} className="text-xs">{p.status}</Badge></div>))}</CardContent></Card> </div> ); }
