import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function CRMDealer() { const { data: dealers = [], isLoading } = useQuery({ queryKey: ["/api/mfg-dealers"],  }); const active = dealers.filter((d: any) => d.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> CRM & Dealer Management </h1> <p className="text-muted-foreground mt-2">Customer/dealer master, leads, opportunities, performance, incentives, complaints</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Dealers</p><p className="text-2xl font-bold">{dealers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{dealers.filter((d: any) => d.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{dealers.length > 0 ? ((active / dealers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Dealers</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : dealers.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : dealers.slice(0, 10).map((d: any) => ( <div key={d.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`dealer-${d.id}`}> <div className="flex-1"><p className="font-semibold">{d.dealerId}</p><p className="text-xs text-muted-foreground">{d.region}</p></div> <Badge variant={d.status === "active" ? "default" : "secondary"} className="text-xs">{d.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
