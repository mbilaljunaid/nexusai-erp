import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Leaf } from "lucide-react"; export default function SustainabilityMaterials() { const { data: materials = [], isLoading } = useQuery({ queryKey: ["/api/fashion-sustainability"], queryFn: () => fetch("/api/fashion-sustainability").then(r => r.json()).catch(() => []), }); const certified = materials.filter((m: any) => m.certified === true).length; const recycled = materials.filter((m: any) => parseInt(m.recycledContent) > 0).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Leaf className="h-8 w-8" /> Sustainability, Traceability & Materials </h1> <p className="text-muted-foreground mt-2">Material provenance, GOTS/OEKO-TEX certs, recycled content %, eco-scores, RSL compliance, QR transparency</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Materials</p><p className="text-2xl font-bold">{materials.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Certified</p><p className="text-2xl font-bold text-green-600">{certified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recycled</p><p className="text-2xl font-bold text-blue-600">{recycled}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Cert Types</p><p className="text-2xl font-bold">{new Set(materials.map((m: any) => m.certType)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Materials</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : materials.length === 0 ? <p className="text-muted-foreground text-center py-4">No materials</p> : materials.slice(0, 10).map((m: any) => ( <div key={m.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`mat-${m.id}`}> <div className="flex-1"><p className="font-semibold">{m.materialId}</p><p className="text-xs text-muted-foreground">Recycled: {m.recycledContent}% • Eco Score: {m.ecoScore}</p></div> <Badge variant={m.certified ? "default" : "secondary"} className="text-xs">{m.certType}</Badge> </div> ))} </CardContent> </Card> </div> ); }
