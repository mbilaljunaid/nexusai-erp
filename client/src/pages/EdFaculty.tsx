import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { GraduationCap } from "lucide-react"; export default function EdFaculty() { const { data: faculty = [], isLoading } = useQuery({ queryKey: ["/api/ed-faculty"],  }); const certified = faculty.filter((f: any) => f.certified).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <GraduationCap className="h-8 w-8" /> Faculty & Staff Management </h1> <p className="text-muted-foreground mt-2">Faculty master, qualifications, credentials, course assignments, performance evaluations, payroll</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Faculty</p><p className="text-2xl font-bold">{faculty.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Certified</p><p className="text-2xl font-bold text-green-600">{certified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-blue-600">{faculty.filter((f: any) => f.status === "active").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Cert %</p><p className="text-2xl font-bold">{faculty.length > 0 ? ((certified / faculty.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Faculty</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : faculty.length === 0 ? <p className="text-muted-foreground text-center py-4">No faculty</p> : faculty.slice(0, 10).map((f: any) => ( <div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`fac-${f.id}`}> <div className="flex-1"><p className="font-semibold">{f.facultyId}</p><p className="text-xs text-muted-foreground">{f.qualification}</p></div> <Badge variant={f.certified ? "default" : "secondary"} className="text-xs">{f.certified ? "Certified" : "Pending"}</Badge> </div> ))} </CardContent> </Card> </div> ); }
