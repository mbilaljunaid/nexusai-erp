import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function HRMfg() { const { data: employees = [], isLoading } = useQuery({ queryKey: ["/api/mfg-hr"],  }); const active = employees.filter((e: any) => e.status === "active").length; const skilled = employees.filter((e: any) => e.skilled).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> HR & Workforce Management </h1> <p className="text-muted-foreground mt-2">Employee master, time/attendance, skills, certifications, scheduling, payroll</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Employees</p><p className="text-2xl font-bold">{employees.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Skilled</p><p className="text-2xl font-bold text-blue-600">{skilled}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Skill %</p><p className="text-2xl font-bold">{employees.length > 0 ? ((skilled / employees.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Employees</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : employees.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : employees.slice(0, 10).map((e: any) => ( <div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`emp-${e.id}`}> <div className="flex-1"><p className="font-semibold">{e.employeeId}</p><p className="text-xs text-muted-foreground">{e.role}</p></div> <Badge variant={e.skilled ? "default" : "secondary"} className="text-xs">{e.skilled ? "Skilled" : "Trainee"}</Badge> </div> ))} </CardContent> </Card> </div> ); }
