import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Grid3x3 } from "lucide-react"; export default function AssortmentPlanner() { const { data: assortments = [], isLoading } = useQuery({ queryKey: ["/api/fashion-assortment"],  }); const active = assortments.filter((a: any) => a.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Grid3x3 className="h-8 w-8" /> Assortment Planning & Allocation </h1> <p className="text-muted-foreground mt-2">By store cluster, channel, geography, size curves, pack config, replenishment rules, markdown planner</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Assortments</p><p className="text-2xl font-bold">{assortments.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Stores</p><p className="text-2xl font-bold">{new Set(assortments.map((a: any) => a.storeCount)).size || 0}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SKUs Allocated</p><p className="text-2xl font-bold">{assortments.reduce((sum: number, a: any) => sum + (parseInt(a.skuCount) || 0), 0)}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Assortments</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : assortments.length === 0 ? <p className="text-muted-foreground text-center py-4">No assortments</p> : assortments.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate" data-testid={`assort-${a.id}`}> <p className="font-semibold">{a.assortmentId}</p> <p className="text-xs text-muted-foreground">Stores: {a.storeCount} • SKUs: {a.skuCount}</p> </div> ))} </CardContent> </Card> </div> ); }
