import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CheckCircle2 } from "lucide-react"; export default function ValidationCSV() { const { data: validations = [], isLoading } = useQuery({ queryKey: ["/api/pharma-validation"], queryFn: () => fetch("/api/pharma-validation").then(r => r.json()).catch(() => []), }); const completed = validations.filter((v: any) => v.status === "completed").length; const inProgress = validations.filter((v: any) => v.status === "in-progress").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CheckCircle2 className="h-8 w-8" /> Validation & CSV (Computer System Validation) </h1> <p className="text-muted-foreground mt-2">IQ/OQ/PQ, DS/FS, traceability matrix, test scripts, 21 CFR Part 11, Annex 11 compliance</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Plans</p><p className="text-2xl font-bold">{validations.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{inProgress}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{validations.length > 0 ? ((completed / validations.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Validation Plans</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : validations.length === 0 ? <p className="text-muted-foreground text-center py-4">No plans</p> : validations.slice(0, 10).map((v: any) => ( <div key={v.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`validation-${v.id}`}> <div className="flex-1"><p className="font-semibold">{v.planId}</p><p className="text-xs text-muted-foreground">Phase: {v.phase}</p></div> <Badge variant={v.status === "completed" ? "default" : "secondary"} className="text-xs">{v.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
