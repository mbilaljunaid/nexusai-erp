import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Calendar } from "lucide-react"; export default function EventsActivities() { const { data: events = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hospitality-events"], queryFn: () => fetch("/api/hospitality-events").then(r => r.json()).catch(() => []), }); const active = events.filter((e: any) => e.status === "active").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Calendar className="h-8 w-8" /> Events & Activities</h1><p className="text-muted-foreground mt-2">Events, tours, activities, registrations</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Events</p><p className="text-2xl font-bold">{events.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-blue-600">{events.filter((e: any) => e.status === "completed").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{events.length > 0 ? ((active / events.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Events</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : events.length === 0 ? <p className="text-muted-foreground text-center py-4">No events</p> : events.slice(0, 10).map((e: any) => (<div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`evt-${e.id}`}><div className="flex-1"><p className="font-semibold">{e.eventId}</p><p className="text-xs text-muted-foreground">{e.type}</p></div><Badge variant={e.status === "active" ? "default" : "secondary"} className="text-xs">{e.status}</Badge></div>))}</CardContent></Card> </div> ); }
