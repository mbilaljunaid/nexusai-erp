import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BookMarked } from "lucide-react"; export default function Gradebook() { const { data: grades = [], isLoading } = useQuery({ queryKey: ["/api/ed-grades"],  }); const passed = grades.filter((g: any) => g.status === "passed").length; const avgGrade = grades.length > 0 ? (grades.reduce((sum: number, g: any) => sum + (parseFloat(g.grade) || 0), 0) / grades.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BookMarked className="h-8 w-8" /> Grade Book & Transcripts </h1> <p className="text-muted-foreground mt-2">Student grades, rubrics, transcripts, GPA, pass/fail, reassessment tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Records</p><p className="text-2xl font-bold">{grades.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Passed</p><p className="text-2xl font-bold text-green-600">{passed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Grade</p><p className="text-2xl font-bold text-blue-600">{avgGrade}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pass %</p><p className="text-2xl font-bold">{grades.length > 0 ? ((passed / grades.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Grades</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : grades.length === 0 ? <p className="text-muted-foreground text-center py-4">No records</p> : grades.slice(0, 10).map((g: any) => ( <div key={g.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`grade-${g.id}`}> <div className="flex-1"><p className="font-semibold">{g.studentId}</p><p className="text-xs text-muted-foreground">{g.courseId}</p></div> <Badge variant={g.status === "passed" ? "default" : "secondary"} className="text-xs">{g.grade}</Badge> </div> ))} </CardContent> </Card> </div> ); }
