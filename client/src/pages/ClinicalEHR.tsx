import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { FileText } from "lucide-react"; export default function ClinicalEHR() { const { data: encounters = [], isLoading } = useQuery({ queryKey: ["/api/hc-encounters"],  }); const completed = encounters.filter((e: any) => e.status === "completed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <FileText className="h-8 w-8" /> Clinical Operations & EHR </h1> <p className="text-muted-foreground mt-2">Encounters, diagnosis, procedures, vital signs, medications, physician notes, care team collaboration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Encounters</p><p className="text-2xl font-bold">{encounters.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{encounters.filter((e: any) => e.status === "in-progress").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{encounters.length > 0 ? ((completed / encounters.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Encounters</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : encounters.length === 0 ? <p className="text-muted-foreground text-center py-4">No encounters</p> : encounters.slice(0, 10).map((e: any) => ( <div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`encounter-${e.id}`}> <div className="flex-1"><p className="font-semibold">{e.encounterId}</p><p className="text-xs text-muted-foreground">{e.diagnosis}</p></div> <Badge variant={e.status === "completed" ? "default" : "secondary"} className="text-xs">{e.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
