import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Beaker } from "lucide-react"; export default function IngredientMasterCPG() { const { data: ingredients = [], isLoading } = useQuery({ queryKey: ["/api/cpg-ingredients"],  }); const certified = ingredients.filter((i: any) => i.certified).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Beaker className="h-8 w-8" /> Ingredient & Material Master </h1> <p className="text-muted-foreground mt-2">Raw materials, suppliers, certifications, composition tracking, CoA, regulatory compliance flags</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Ingredients</p><p className="text-2xl font-bold">{ingredients.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Certified</p><p className="text-2xl font-bold text-green-600">{certified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Suppliers</p><p className="text-2xl font-bold">{new Set(ingredients.map((i: any) => i.supplierId)).size}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Cert %</p><p className="text-2xl font-bold text-blue-600">{ingredients.length > 0 ? ((certified / ingredients.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Ingredients</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : ingredients.length === 0 ? <p className="text-muted-foreground text-center py-4">No ingredients</p> : ingredients.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`ingredient-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.ingredientId}</p><p className="text-xs text-muted-foreground">Supplier: {i.supplierId}</p></div> <Badge variant={i.certified ? "default" : "secondary"} className="text-xs">{i.type}</Badge> </div> ))} </CardContent> </Card> </div> ); }
