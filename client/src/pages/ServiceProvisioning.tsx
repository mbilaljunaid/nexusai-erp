import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Network } from "lucide-react"; export default function ServiceProvisioning() { const { data: services = [], isLoading } = useQuery({ queryKey: ["/api/telecom-services"], queryFn: () => fetch("/api/telecom-services").then(r => r.json()).catch(() => []), }); const active = services.filter((s: any) => s.status === "active").length; const slaCompliant = services.filter((s: any) => s.slaCompliance > 95).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Network className="h-8 w-8" /> Service Provisioning & Network </h1> <p className="text-muted-foreground mt-2">Service activation, suspension, termination, usage monitoring, QoS, SLA tracking, fault reporting</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Services</p><p className="text-2xl font-bold">{services.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SLA Compliant</p><p className="text-2xl font-bold text-blue-600">{slaCompliant}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliance %</p><p className="text-2xl font-bold">{services.length > 0 ? ((slaCompliant / services.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Services</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : services.length === 0 ? <p className="text-muted-foreground text-center py-4">No services</p> : services.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`service-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.serviceId}</p><p className="text-xs text-muted-foreground">SLA: {s.slaCompliance}%</p></div> <Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
