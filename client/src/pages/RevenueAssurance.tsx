import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { DollarSign } from "lucide-react"; export default function RevenueAssurance() { const { data: revenue = [], isLoading } = useQuery({ queryKey: ["/api/telecom-revenue"],  }); const totalRev = revenue.reduce((sum: number, r: any) => sum + (parseFloat(r.amount) || 0), 0); const projected = revenue.reduce((sum: number, r: any) => sum + (parseFloat(r.projected) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <DollarSign className="h-8 w-8" /> Revenue Assurance & Forecasting </h1> <p className="text-muted-foreground mt-2">Revenue tracking, forecasting, promotion ROI, marketing spend, billing accuracy, trend analysis</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Revenue</p><p className="text-2xl font-bold text-green-600">${(totalRev / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Projected</p><p className="text-2xl font-bold text-blue-600">${(projected / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Variance</p><p className="text-2xl font-bold">${((projected - totalRev) / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Accuracy %</p><p className="text-2xl font-bold">{revenue.length > 0 ? ((totalRev / projected) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Revenue Tracking</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : revenue.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : revenue.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`rev-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.sourceId}</p><p className="text-xs text-muted-foreground">Projected: ${r.projected}</p></div> <Badge variant="default" className="text-xs">${r.amount}</Badge> </div> ))} </CardContent> </Card> </div> ); }
