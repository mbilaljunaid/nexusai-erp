import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Truck } from "lucide-react"; export default function FleetManagement() { const { data: vehicles = [], isLoading } = useQuery({ queryKey: ["/api/logistics-fleet"],  }); const active = vehicles.filter((v: any) => v.status === "active").length; const maintenance = vehicles.filter((v: any) => v.status === "maintenance").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Truck className="h-8 w-8" /> Fleet & Vehicle Management </h1> <p className="text-muted-foreground mt-2">Vehicles, registration, maintenance, drivers, fuel, utilization</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Vehicles</p><p className="text-2xl font-bold">{vehicles.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Maintenance</p><p className="text-2xl font-bold text-yellow-600">{maintenance}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilization %</p><p className="text-2xl font-bold">{vehicles.length > 0 ? ((active / vehicles.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Fleet Status</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : vehicles.length === 0 ? <p className="text-muted-foreground text-center py-4">No vehicles</p> : vehicles.slice(0, 10).map((v: any) => ( <div key={v.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`vehicle-${v.id}`}> <div className="flex-1"><p className="font-semibold">{v.vehicleId}</p><p className="text-xs text-muted-foreground">{v.type}</p></div> <Badge variant={v.status === "active" ? "default" : "secondary"} className="text-xs">{v.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
