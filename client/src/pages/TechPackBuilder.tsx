import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Ruler } from "lucide-react"; export default function TechPackBuilder() { const { data: packs = [], isLoading } = useQuery({ queryKey: ["/api/fashion-techpack"], queryFn: () => fetch("/api/fashion-techpack").then(r => r.json()).catch(() => []), }); const approved = packs.filter((p: any) => p.status === "approved").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Ruler className="h-8 w-8" /> Tech Pack Builder </h1> <p className="text-muted-foreground mt-2">Measurements, grade rules, tolerances, trims, BOM, costing, CAD links, design change requests (ECO)</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Tech Packs</p><p className="text-2xl font-bold">{packs.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Draft</p><p className="text-2xl font-bold text-yellow-600">{packs.length - approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approval %</p><p className="text-2xl font-bold">{packs.length > 0 ? ((approved / packs.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Tech Packs</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : packs.length === 0 ? <p className="text-muted-foreground text-center py-4">No packs</p> : packs.slice(0, 10).map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`pack-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.packId}</p><p className="text-xs text-muted-foreground">Sizes: {p.sizeRange}</p></div> <Badge variant={p.status === "approved" ? "default" : "secondary"} className="text-xs">{p.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
