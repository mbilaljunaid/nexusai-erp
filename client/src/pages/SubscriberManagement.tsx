import { useState } from "react"; import { useQuery, useMutation } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Badge } from "@/components/ui/badge"; import { Users, Plus, Trash2 } from "lucide-react"; import { queryClient } from "@/lib/queryClient"; import { useToast } from "@/hooks/use-toast"; export default function SubscriberManagement() { const { toast } = useToast(); const [newSub, setNewSub] = useState({ customerId: "", name: "", email: "", phone: "", status: "active" }); const { data: subscribers = [], isLoading } = useQuery({ queryKey: ["/api/telecom-subscribers"], queryFn: () => fetch("/api/telecom-subscribers").then(r => r.json()).catch(() => []), }); const createMutation = useMutation({ mutationFn: (data: any) => fetch("/api/telecom-subscribers", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }).then(r => r.json()), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/telecom-subscribers"] }); setNewSub({ customerId: "", name: "", email: "", phone: "", status: "active" }); toast({ title: "Subscriber added" }); }, }); const deleteMutation = useMutation({ mutationFn: (id: string) => fetch(`/api/telecom-subscribers/${id}`, { method: "DELETE" }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/telecom-subscribers"] }); toast({ title: "Subscriber deleted" }); }, }); const active = subscribers.filter((s: any) => s.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> Subscriber & Customer Management </h1> <p className="text-muted-foreground mt-2">Customer master, KYC, account lifecycle, tier management, contact management, multi-line accounts</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Subscribers</p><p className="text-2xl font-bold">{subscribers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Suspended</p><p className="text-2xl font-bold text-yellow-600">{subscribers.filter((s: any) => s.status === "suspended").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Activation %</p><p className="text-2xl font-bold">{subscribers.length > 0 ? ((active / subscribers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card data-testid="card-new-sub"> <CardHeader><CardTitle className="text-base">Add Subscriber</CardTitle></CardHeader> <CardContent className="space-y-3"> <div className="grid grid-cols-5 gap-2"> <Input placeholder="ID" value={newSub.customerId} onChange={(e) => setNewSub({ ...newSub, customerId: e.target.value })} data-testid="input-cid" className="text-sm" /> <Input placeholder="Name" value={newSub.name} onChange={(e) => setNewSub({ ...newSub, name: e.target.value })} data-testid="input-name" className="text-sm" /> <Input placeholder="Email" value={newSub.email} onChange={(e) => setNewSub({ ...newSub, email: e.target.value })} data-testid="input-email" className="text-sm" /> <Input placeholder="Phone" value={newSub.phone} onChange={(e) => setNewSub({ ...newSub, phone: e.target.value })} data-testid="input-phone" className="text-sm" /> <Button disabled={createMutation.isPending || !newSub.customerId} size="sm" data-testid="button-add"> <Plus className="w-3 h-3" /> </Button> </div> </CardContent> </Card> <Card> <CardHeader><CardTitle className="text-base">Subscribers</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : subscribers.length === 0 ? <p className="text-muted-foreground text-center py-4">No subscribers</p> : subscribers.map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`sub-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.customerId}</p><p className="text-xs text-muted-foreground">{s.phone}</p></div> <div className="flex gap-2 items-center"> <Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> <Button size="icon" variant="ghost" data-testid={`btn-del-${s.id}`} className="h-7 w-7"> <Trash2 className="w-3 h-3" /> </Button> </div> </div> ))} </CardContent> </Card> </div> ); }
