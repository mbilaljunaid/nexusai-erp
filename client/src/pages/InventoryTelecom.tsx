import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Box } from "lucide-react"; export default function InventoryTelecom() { const { data: assets = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-inventory"], queryFn: () => fetch("/api/telecom-inventory").then(r => r.json()).catch(() => []), }); const active = assets.filter((a: any) => a.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Box className="h-8 w-8" /> Inventory & Asset Management </h1> <p className="text-muted-foreground mt-2">Equipment master, spare parts, locations, asset lifecycle</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Assets</p><p className="text-2xl font-bold">{assets.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Spare Parts</p><p className="text-2xl font-bold text-blue-600">{assets.filter((a: any) => a.type === "spare").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilization %</p><p className="text-2xl font-bold">{assets.length > 0 ? ((active / assets.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Assets</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : assets.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : assets.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`asset-${a.id}`}> <div className="flex-1"><p className="font-semibold">{a.assetId}</p><p className="text-xs text-muted-foreground">{a.type}</p></div> <Badge variant={a.status === "active" ? "default" : "secondary"} className="text-xs">{a.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
