import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Tag } from "lucide-react"; export default function PricingPromotions() { const { data: promos = [], isLoading } = useQuery({ queryKey: ["/api/retail-promos"],  }); const active = promos.filter((p: any) => p.status === "active").length; const totalBudget = promos.reduce((sum: number, p: any) => sum + (parseFloat(p.budget) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Tag className="h-8 w-8" /> Pricing, Promotions & Discounts </h1> <p className="text-muted-foreground mt-2">Price books, tiered pricing, promo campaigns, coupons, loyalty, regional pricing</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Promotions</p><p className="text-2xl font-bold">{promos.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Budget</p><p className="text-2xl font-bold text-blue-600">${(totalBudget / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{promos.length > 0 ? ((active / promos.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Promotions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : promos.length === 0 ? <p className="text-muted-foreground text-center py-4">No promos</p> : promos.slice(0, 10).map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`promo-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.promoId}</p><p className="text-xs text-muted-foreground">{p.discountType}</p></div> <Badge variant={p.status === "active" ? "default" : "secondary"} className="text-xs">{p.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
