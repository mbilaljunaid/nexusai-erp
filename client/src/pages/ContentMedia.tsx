import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Film } from "lucide-react"; export default function ContentMedia() { const { data: content = [], isLoading } = useQuery({ queryKey: ["/api/telecom-content"],  }); const licensed = content.filter((c: any) => c.licensed).length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Film className="h-8 w-8" /> Content & Media</h1><p className="text-muted-foreground mt-2">Catalog, rights, licensing, distribution, scheduling</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Content</p><p className="text-2xl font-bold">{content.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Licensed</p><p className="text-2xl font-bold text-green-600">{licensed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Types</p><p className="text-2xl font-bold text-blue-600">{new Set(content.map((c: any) => c.type)).size}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Licensed %</p><p className="text-2xl font-bold">{content.length > 0 ? ((licensed / content.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Content</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : content.length === 0 ? <p className="text-muted-foreground text-center py-4">No content</p> : content.slice(0, 10).map((c: any) => (<div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`cont-${c.id}`}><div className="flex-1"><p className="font-semibold">{c.title}</p><p className="text-xs text-muted-foreground">{c.type}</p></div><Badge variant={c.licensed ? "default" : "secondary"} className="text-xs">{c.licensed ? "Licensed" : "Pending"}</Badge></div>))}</CardContent></Card> </div> ); }
