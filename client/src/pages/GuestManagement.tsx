import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function GuestManagement() { const { data: guests = [], isLoading } = useQuery({ queryKey: ["/api/hospitality-guests"],  }); const vip = guests.filter((g: any) => g.tier === "vip").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Users className="h-8 w-8" /> Guest Management</h1><p className="text-muted-foreground mt-2">Profiles, preferences, requests, feedback</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Guests</p><p className="text-2xl font-bold">{guests.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">VIP</p><p className="text-2xl font-bold text-green-600">{vip}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Regular</p><p className="text-2xl font-bold text-blue-600">{guests.filter((g: any) => g.tier === "regular").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">VIP %</p><p className="text-2xl font-bold">{guests.length > 0 ? ((vip / guests.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Guests</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : guests.length === 0 ? <p className="text-muted-foreground text-center py-4">No guests</p> : guests.slice(0, 10).map((g: any) => (<div key={g.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`guest-${g.id}`}><div className="flex-1"><p className="font-semibold">{g.guestId}</p><p className="text-xs text-muted-foreground">{g.tier}</p></div><Badge variant="default" className="text-xs">{g.status}</Badge></div>))}</CardContent></Card> </div> ); }
