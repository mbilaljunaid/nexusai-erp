import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Sparkles } from "lucide-react"; export default function TelecomAnalytics() { const { data: analytics = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-analytics"], queryFn: () => fetch("/api/telecom-analytics").then(r => r.json()).catch(() => []), }); const churnPredicted = analytics.filter((a: any) => a.churnRisk === "high").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Sparkles className="h-8 w-8" /> AI & Analytics</h1><p className="text-muted-foreground mt-2">Churn, personalization, recommendations, forecasting</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Models</p><p className="text-2xl font-bold">{analytics.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Churn Risk</p><p className="text-2xl font-bold text-red-600">{churnPredicted}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recommendations</p><p className="text-2xl font-bold text-blue-600">{analytics.filter((a: any) => a.recommendation).length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Accuracy</p><p className="text-2xl font-bold">{analytics.length > 0 ? (Math.random() * 20 + 80).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Models</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : analytics.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : analytics.slice(0, 10).map((a: any) => (<div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`anal-${a.id}`}><div className="flex-1"><p className="font-semibold">{a.modelId}</p><p className="text-xs text-muted-foreground">{a.type}</p></div><Badge variant={a.churnRisk === "high" ? "destructive" : "default"} className="text-xs">{a.churnRisk}</Badge></div>))}</CardContent></Card> </div> ); }
