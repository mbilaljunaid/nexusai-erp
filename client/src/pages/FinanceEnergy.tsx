import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { DollarSign } from "lucide-react"; export default function FinanceEnergy() { const { data: finance = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/energy-finance"], queryFn: () => fetch("/api/energy-finance").then(r => r.json()).catch(() => []), }); const revenue = finance.reduce((sum: number, f: any) => sum + (parseFloat(f.revenue) || 0), 0); const expense = finance.reduce((sum: number, f: any) => sum + (parseFloat(f.expense) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><DollarSign className="h-8 w-8" /> Finance & EPM</h1><p className="text-muted-foreground mt-2">GL, budgets, P&L, cost tracking, variance reporting</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Transactions</p><p className="text-2xl font-bold">{finance.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Revenue</p><p className="text-2xl font-bold text-green-600">${(revenue / 1000000).toFixed(0)}M</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Expenses</p><p className="text-2xl font-bold text-red-600">${(expense / 1000000).toFixed(0)}M</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Net Income</p><p className="text-2xl font-bold text-blue-600">${((revenue - expense) / 1000000).toFixed(0)}M</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Financial Data</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : finance.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : finance.slice(0, 10).map((f: any) => (<div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`fin-${f.id}`}><div className="flex-1"><p className="font-semibold">{f.accountId}</p><p className="text-xs text-muted-foreground">${f.revenue}</p></div><Badge variant="default" className="text-xs">Active</Badge></div>))}</CardContent></Card> </div> ); }
