import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function FinanceHealthcare() { const { data: finance = [], isLoading } = useQuery({ queryKey: ["/api/hc-finance"],  }); const totalRev = finance.reduce((sum: number, f: any) => sum + (parseFloat(f.revenue) || 0), 0); const totalExp = finance.reduce((sum: number, f: any) => sum + (parseFloat(f.expense) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BarChart3 className="h-8 w-8" /> Finance & EPM </h1> <p className="text-muted-foreground mt-2">GL, AP, AR, budgeting, cost centers, revenue forecasting, expense allocation, variance reporting</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Revenue</p><p className="text-2xl font-bold text-green-600">${(totalRev / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Expense</p><p className="text-2xl font-bold text-red-600">${(totalExp / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Net</p><p className="text-2xl font-bold text-blue-600">${((totalRev - totalExp) / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Margin %</p><p className="text-2xl font-bold">{totalRev > 0 ? (((totalRev - totalExp) / totalRev) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Financial Summary</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : finance.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : finance.slice(0, 10).map((f: any) => ( <div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`fin-${f.id}`}> <div className="flex-1"><p className="font-semibold">{f.departmentId}</p><p className="text-xs text-muted-foreground">Rev: ${f.revenue}</p></div> <Badge variant="default" className="text-xs">${f.revenue}</Badge> </div> ))} </CardContent> </Card> </div> ); }
