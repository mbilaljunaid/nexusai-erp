import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Wrench } from "lucide-react"; export default function Maintenance() { const { data: maintenance = [], isLoading } = useQuery({ queryKey: ["/api/mfg-maintenance"],  }); const completed = maintenance.filter((m: any) => m.status === "completed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Wrench className="h-8 w-8" /> Maintenance & Asset Management </h1> <p className="text-muted-foreground mt-2">Equipment, PM scheduling, CM logging, spare parts, downtime tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Tickets</p><p className="text-2xl font-bold">{maintenance.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{maintenance.filter((m: any) => m.status === "in-progress").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Complete %</p><p className="text-2xl font-bold">{maintenance.length > 0 ? ((completed / maintenance.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Maintenance</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : maintenance.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : maintenance.slice(0, 10).map((m: any) => ( <div key={m.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`maint-${m.id}`}> <div className="flex-1"><p className="font-semibold">{m.maintenanceId}</p><p className="text-xs text-muted-foreground">{m.assetId}</p></div> <Badge variant={m.status === "completed" ? "default" : "secondary"} className="text-xs">{m.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
