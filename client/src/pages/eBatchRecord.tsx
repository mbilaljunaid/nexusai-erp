import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { FileText } from "lucide-react"; export default function eBatchRecord() { const { data: batches = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/pharma-ebr"], queryFn: () => fetch("/api/pharma-ebr").then(r => r.json()).catch(() => []), }); const completed = batches.filter((b: any) => b.status === "completed").length; const inExecution = batches.filter((b: any) => b.status === "in-progress").length; const onHold = batches.filter((b: any) => b.status === "on-hold").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <FileText className="h-8 w-8" /> Electronic Batch Record (eBR) </h1> <p className="text-muted-foreground mt-2">S88 workflow, step-wise instructions, digital signatures, deviations, QC holds, MES integration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Batches</p><p className="text-2xl font-bold">{batches.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-blue-600">{inExecution}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">On Hold</p><p className="text-2xl font-bold text-orange-600">{onHold}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Batch Records</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : batches.length === 0 ? <p className="text-muted-foreground text-center py-4">No batches</p> : batches.slice(0, 10).map((b: any) => ( <div key={b.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`batch-${b.id}`}> <div className="flex-1"><p className="font-semibold">{b.batchId}</p><p className="text-xs text-muted-foreground">Recipe v{b.recipeVersion} • Qty: {b.quantity}</p></div> <Badge variant={b.status === "completed" ? "default" : b.status === "on-hold" ? "destructive" : "secondary"} className="text-xs">{b.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
