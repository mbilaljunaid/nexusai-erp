import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Package } from "lucide-react"; export default function SupplyChainHealthcare() { const { data: supply = [], isLoading } = useQuery({ queryKey: ["/api/hc-supply"], queryFn: () => fetch("/api/hc-supply").then(r => r.json()).catch(() => []), }); const received = supply.filter((s: any) => s.status === "received").length; const pendingQc = supply.filter((s: any) => s.status === "pending-qc").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Package className="h-8 w-8" /> Supply Chain & Procurement </h1> <p className="text-muted-foreground mt-2">Suppliers, PO, receiving, cold chain, lot traceability, recall management, equipment procurement</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Orders</p><p className="text-2xl font-bold">{supply.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Received</p><p className="text-2xl font-bold text-green-600">{received}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending QC</p><p className="text-2xl font-bold text-yellow-600">{pendingQc}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Receipt %</p><p className="text-2xl font-bold">{supply.length > 0 ? ((received / supply.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Orders</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : supply.length === 0 ? <p className="text-muted-foreground text-center py-4">No orders</p> : supply.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`supply-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.poId}</p><p className="text-xs text-muted-foreground">{s.supplierId}</p></div> <Badge variant={s.status === "received" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
