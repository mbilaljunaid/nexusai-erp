import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function HRRetail() { const { data: staff = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-hr"], queryFn: () => fetch("/api/retail-hr").then(r => r.json()).catch(() => []), }); const onShift = staff.filter((s: any) => s.status === "on-shift").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> HR & Workforce Management </h1> <p className="text-muted-foreground mt-2">Employee master, payroll, recruitment, shift scheduling, attendance, performance tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Staff</p><p className="text-2xl font-bold">{staff.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">On Shift</p><p className="text-2xl font-bold text-green-600">{onShift}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Off Shift</p><p className="text-2xl font-bold text-yellow-600">{staff.filter((s: any) => s.status === "off-shift").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Utilization %</p><p className="text-2xl font-bold">{staff.length > 0 ? ((onShift / staff.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Staff</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : staff.length === 0 ? <p className="text-muted-foreground text-center py-4">No staff</p> : staff.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`staff-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.employeeId}</p><p className="text-xs text-muted-foreground">{s.role}</p></div> <Badge variant={s.status === "on-shift" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
