import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Megaphone } from "lucide-react"; export default function MarketingCampaigns() { const { data: campaigns = [], isLoading } = useQuery({ queryKey: ["/api/fashion-campaigns"], queryFn: () => fetch("/api/fashion-campaigns").then(r => r.json()).catch(() => []), }); const active = campaigns.filter((c: any) => c.status === "active").length; const avgConversion = campaigns.length > 0 ? (campaigns.reduce((sum: number, c: any) => sum + (parseFloat(c.conversion) || 0), 0) / campaigns.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Megaphone className="h-8 w-8" /> Marketing, Lookbooks & Influencer Campaigns </h1> <p className="text-muted-foreground mt-2">Campaign builder, drops, lookbooks, promo codes, influencer tracking, VIP early access, CRM segments</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Campaigns</p><p className="text-2xl font-bold">{campaigns.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Conversion</p><p className="text-2xl font-bold text-blue-600">{avgConversion}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Influencers</p><p className="text-2xl font-bold">{new Set(campaigns.map((c: any) => c.influencers)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Campaigns</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : campaigns.length === 0 ? <p className="text-muted-foreground text-center py-4">No campaigns</p> : campaigns.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`campaign-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.campaignId}</p><p className="text-xs text-muted-foreground">Drop: {c.dropDate}</p></div> <Badge variant={c.status === "active" ? "default" : "secondary"} className="text-xs">{c.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
