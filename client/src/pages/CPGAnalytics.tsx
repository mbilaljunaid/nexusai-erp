import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function CPGAnalytics() { const { data: analytics = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-analytics"], queryFn: () => fetch("/api/cpg-analytics").then(r => r.json()).catch(() => []), }); const avgSellThru = analytics.length > 0 ? (analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.sellThru) || 0), 0) / analytics.length).toFixed(1) : 0; const avgReturnRate = analytics.length > 0 ? (analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.returnRate) || 0), 0) / analytics.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BarChart3 className="h-8 w-8" /> CPG BI & Dashboards </h1> <p className="text-muted-foreground mt-2">Sales vs forecast, inventory aging, returns trends, promotion ROI, production efficiency, KPI tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SKUs</p><p className="text-2xl font-bold">{analytics.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Sell-Through</p><p className="text-2xl font-bold text-green-600">{avgSellThru}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Return Rate</p><p className="text-2xl font-bold text-orange-600">{avgReturnRate}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Channels</p><p className="text-2xl font-bold">{new Set(analytics.map((a: any) => a.channel)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Analytics</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : analytics.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : analytics.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`analytics-${a.id}`}> <div className="flex-1"><p className="font-semibold">{a.sku}</p><p className="text-xs text-muted-foreground">Sell-Through: {a.sellThru}% • Return: {a.returnRate}%</p></div> <Badge variant={a.sellThru > 70 ? "default" : "secondary"} className="text-xs">{a.channel}</Badge> </div> ))} </CardContent> </Card> </div> ); }
