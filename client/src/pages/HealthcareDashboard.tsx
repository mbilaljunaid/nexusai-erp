import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Activity } from "lucide-react"; export default function HealthcareDashboard() { const { data: dashboard = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hc-dashboard"], queryFn: () => fetch("/api/hc-dashboard").then(r => r.json()).catch(() => []), }); const totalKpis = dashboard.length; const onTrack = dashboard.filter((k: any) => k.status === "on-track").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Activity className="h-8 w-8" /> Healthcare Operations Dashboard </h1> <p className="text-muted-foreground mt-2">Executive summary, KPIs, operational metrics, compliance status, financial health, alerts & trends</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">KPIs</p><p className="text-2xl font-bold">{totalKpis}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">On Track</p><p className="text-2xl font-bold text-green-600">{onTrack}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">At Risk</p><p className="text-2xl font-bold text-orange-600">{dashboard.filter((k: any) => k.status === "at-risk").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Health %</p><p className="text-2xl font-bold">{totalKpis > 0 ? ((onTrack / totalKpis) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Key Metrics</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : dashboard.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : dashboard.slice(0, 10).map((k: any) => ( <div key={k.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`kpi-${k.id}`}> <div className="flex-1"><p className="font-semibold">{k.kpiName}</p><p className="text-xs text-muted-foreground">Target: {k.target}</p></div> <p className="font-semibold">{k.actual}</p> </div> ))} </CardContent> </Card> </div> ); }
