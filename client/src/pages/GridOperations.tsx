import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Zap } from "lucide-react"; export default function GridOperations() { const { data: network = [], isLoading } = useQuery({ queryKey: ["/api/energy-grid"],  }); const healthy = network.filter((n: any) => n.status === "healthy").length; const totalLoad = network.reduce((sum: number, n: any) => sum + (parseFloat(n.load) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Zap className="h-8 w-8" /> Grid & Network Operations</h1><p className="text-muted-foreground mt-2">Transmission, distribution, load balancing, outages, SCADA/EMS</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Substations</p><p className="text-2xl font-bold">{network.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Healthy</p><p className="text-2xl font-bold text-green-600">{healthy}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Load</p><p className="text-2xl font-bold text-blue-600">{(totalLoad / 1000).toFixed(0)}K MW</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Health %</p><p className="text-2xl font-bold">{network.length > 0 ? ((healthy / network.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Network Status</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : network.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : network.slice(0, 10).map((n: any) => (<div key={n.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`net-${n.id}`}><div className="flex-1"><p className="font-semibold">{n.substationId}</p><p className="text-xs text-muted-foreground">{n.load} MW</p></div><Badge variant={n.status === "healthy" ? "default" : "secondary"} className="text-xs">{n.status}</Badge></div>))}</CardContent></Card> </div> ); }
