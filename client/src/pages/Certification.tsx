import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Award } from "lucide-react"; export default function Certification() { const { data: certs = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/ed-certificates"], queryFn: () => fetch("/api/ed-certificates").then(r => r.json()).catch(() => []), }); const issued = certs.filter((c: any) => c.status === "issued").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Award className="h-8 w-8" /> Certificate & Credential Issuance </h1> <p className="text-muted-foreground mt-2">Digital badges, PDF certificates, credential verification, issuance tracking, renewal management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Certificates</p><p className="text-2xl font-bold">{certs.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Issued</p><p className="text-2xl font-bold text-green-600">{issued}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{certs.filter((c: any) => c.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Issue %</p><p className="text-2xl font-bold">{certs.length > 0 ? ((issued / certs.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Certificates</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : certs.length === 0 ? <p className="text-muted-foreground text-center py-4">No certificates</p> : certs.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`cert-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.certificateId}</p><p className="text-xs text-muted-foreground">{c.courseId}</p></div> <Badge variant={c.status === "issued" ? "default" : "secondary"} className="text-xs">{c.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
