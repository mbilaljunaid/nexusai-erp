import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function GovernmentHR() { const { data: employees = [], isLoading } = useQuery({ queryKey: ["/api/gov-hr"], queryFn: () => fetch("/api/gov-hr").then(r => r.json()).catch(() => []), }); const active = employees.filter((e: any) => e.status === "active").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Users className="h-8 w-8" /> HR & Workforce</h1><p className="text-muted-foreground mt-2">Civil servants, roles, attendance, payroll</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Employees</p><p className="text-2xl font-bold">{employees.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Departments</p><p className="text-2xl font-bold text-blue-600">{new Set(employees.map((e: any) => e.departmentId)).size}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{employees.length > 0 ? ((active / employees.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Staff</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : employees.length === 0 ? <p className="text-muted-foreground text-center py-4">No employees</p> : employees.slice(0, 10).map((e: any) => (<div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`emp-${e.id}`}><div className="flex-1"><p className="font-semibold">{e.employeeId}</p><p className="text-xs text-muted-foreground">{e.departmentId}</p></div><Badge variant={e.status === "active" ? "default" : "secondary"} className="text-xs">{e.status}</Badge></div>))}</CardContent></Card> </div> ); }
