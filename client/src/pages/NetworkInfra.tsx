import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Network } from "lucide-react"; export default function NetworkInfra() { const { data: elements = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-network"], queryFn: () => fetch("/api/telecom-network").then(r => r.json()).catch(() => []), }); const healthy = elements.filter((e: any) => e.status === "healthy").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Network className="h-8 w-8" /> Network & Infrastructure</h1><p className="text-muted-foreground mt-2">Topology, faults, capacity, maintenance</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Elements</p><p className="text-2xl font-bold">{elements.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Healthy</p><p className="text-2xl font-bold text-green-600">{healthy}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Faults</p><p className="text-2xl font-bold text-red-600">{elements.filter((e: any) => e.status === "fault").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Health %</p><p className="text-2xl font-bold">{elements.length > 0 ? ((healthy / elements.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Network Elements</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : elements.length === 0 ? <p className="text-muted-foreground text-center py-4">No elements</p> : elements.slice(0, 10).map((e: any) => (<div key={e.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`net-${e.id}`}><div className="flex-1"><p className="font-semibold">{e.elementId}</p><p className="text-xs text-muted-foreground">{e.type}</p></div><Badge variant={e.status === "healthy" ? "default" : "destructive"} className="text-xs">{e.status}</Badge></div>))}</CardContent></Card> </div> ); }
