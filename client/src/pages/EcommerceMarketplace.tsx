import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Globe } from "lucide-react"; export default function EcommerceMarketplace() { const { data: listings = [], isLoading } = useQuery({ queryKey: ["/api/fashion-ecom"], queryFn: () => fetch("/api/fashion-ecom").then(r => r.json()).catch(() => []), }); const active = listings.filter((l: any) => l.status === "active").length; const totalSales = listings.reduce((sum: number, l: any) => sum + (parseInt(l.sales) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Globe className="h-8 w-8" /> E-Commerce & Marketplace Integration </h1> <p className="text-muted-foreground mt-2">PDP with fit recommender, 3D try-on, pre-order/drops, marketplace sync (Amazon, Zalando), click&collect</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Listings</p><p className="text-2xl font-bold">{listings.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Sales</p><p className="text-2xl font-bold">${totalSales}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Marketplaces</p><p className="text-2xl font-bold">{new Set(listings.map((l: any) => l.marketplace)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Marketplace Listings</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : listings.length === 0 ? <p className="text-muted-foreground text-center py-4">No listings</p> : listings.slice(0, 10).map((l: any) => ( <div key={l.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`listing-${l.id}`}> <div className="flex-1"><p className="font-semibold">{l.sku}</p><p className="text-xs text-muted-foreground">{l.marketplace}</p></div> <Badge variant={l.status === "active" ? "default" : "secondary"} className="text-xs">${l.sales}</Badge> </div> ))} </CardContent> </Card> </div> ); }
