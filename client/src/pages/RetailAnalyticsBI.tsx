import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function RetailAnalyticsBI() { const { data: analytics = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/retail-analytics"], queryFn: () => fetch("/api/retail-analytics").then(r => r.json()).catch(() => []), }); const totalRevenue = analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.revenue) || 0), 0); const avgMargin = analytics.length > 0 ? (analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.margin) || 0), 0) / analytics.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><BarChart3 className="h-8 w-8" /> Retail BI & Dashboards</h1><p className="text-muted-foreground mt-2">Sales, products, customers, campaigns, KPIs</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Datasets</p><p className="text-2xl font-bold">{analytics.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Revenue</p><p className="text-2xl font-bold text-green-600">${(totalRevenue / 1000000).toFixed(0)}M</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Margin</p><p className="text-2xl font-bold text-blue-600">{avgMargin}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Channels</p><p className="text-2xl font-bold">{new Set(analytics.map((a: any) => a.channel)).size}</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Analytics</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : analytics.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : analytics.slice(0, 10).map((a: any) => (<div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`anal-${a.id}`}><div className="flex-1"><p className="font-semibold">{a.channel}</p><p className="text-xs text-muted-foreground">${a.revenue}</p></div><Badge variant="default" className="text-xs">{a.margin}%</Badge></div>))}</CardContent></Card> </div> ); }
