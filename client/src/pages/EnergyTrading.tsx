import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { TrendingUp } from "lucide-react"; export default function EnergyTrading() { const { data: trades = [], isLoading } = useQuery({ queryKey: ["/api/energy-trading"],  }); const settled = trades.filter((t: any) => t.status === "settled").length; const totalValue = trades.reduce((sum: number, t: any) => sum + (parseFloat(t.value) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><TrendingUp className="h-8 w-8" /> Energy & Resource Trading</h1><p className="text-muted-foreground mt-2">Commodities, contracts, pricing, hedging, risk management</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Trades</p><p className="text-2xl font-bold">{trades.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Settled</p><p className="text-2xl font-bold text-green-600">{settled}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Value</p><p className="text-2xl font-bold text-blue-600">${(totalValue / 1000000).toFixed(0)}M</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Settlement %</p><p className="text-2xl font-bold">{trades.length > 0 ? ((settled / trades.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Trading Book</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : trades.length === 0 ? <p className="text-muted-foreground text-center py-4">No trades</p> : trades.slice(0, 10).map((t: any) => (<div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`trade-${t.id}`}><div className="flex-1"><p className="font-semibold">{t.tradeId}</p><p className="text-xs text-muted-foreground">${t.value}</p></div><Badge variant={t.status === "settled" ? "default" : "secondary"} className="text-xs">{t.status}</Badge></div>))}</CardContent></Card> </div> ); }
