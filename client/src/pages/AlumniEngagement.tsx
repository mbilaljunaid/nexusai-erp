import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function AlumniEngagement() { const { data: alumni = [], isLoading } = useQuery({ queryKey: ["/api/ed-alumni"],  }); const engaged = alumni.filter((a: any) => a.engagementStatus === "active").length; const employed = alumni.filter((a: any) => a.employmentStatus === "employed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> CRM, Alumni & Student Engagement </h1> <p className="text-muted-foreground mt-2">Alumni campaigns, feedback, surveys, scholarships, career services, job placement tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Alumni</p><p className="text-2xl font-bold">{alumni.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Engaged</p><p className="text-2xl font-bold text-green-600">{engaged}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Employed</p><p className="text-2xl font-bold text-blue-600">{employed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Employment %</p><p className="text-2xl font-bold">{alumni.length > 0 ? ((employed / alumni.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Alumni</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : alumni.length === 0 ? <p className="text-muted-foreground text-center py-4">No alumni</p> : alumni.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`alumni-${a.id}`}> <div className="flex-1"><p className="font-semibold">{a.alumniId}</p><p className="text-xs text-muted-foreground">{a.employmentStatus}</p></div> <Badge variant={a.engagementStatus === "active" ? "default" : "secondary"} className="text-xs">{a.engagementStatus}</Badge> </div> ))} </CardContent> </Card> </div> ); }
