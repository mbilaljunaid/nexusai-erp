import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Microscope } from "lucide-react"; export default function LaboratoryDiagnostics() { const { data: tests = [], isLoading } = useQuery({ queryKey: ["/api/hc-labs"],  }); const completed = tests.filter((t: any) => t.status === "completed").length; const avgTat = tests.length > 0 ? (tests.reduce((sum: number, t: any) => sum + (parseFloat(t.tatHours) || 0), 0) / tests.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Microscope className="h-8 w-8" /> Laboratory & Diagnostics </h1> <p className="text-muted-foreground mt-2">Test catalog, orders, specimen tracking, results, QC, device integration, HL7/FHIR</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Tests</p><p className="text-2xl font-bold">{tests.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg TAT (hrs)</p><p className="text-2xl font-bold text-blue-600">{avgTat}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{tests.length > 0 ? ((completed / tests.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Lab Tests</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : tests.length === 0 ? <p className="text-muted-foreground text-center py-4">No tests</p> : tests.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`test-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.testId}</p><p className="text-xs text-muted-foreground">TAT: {t.tatHours}h</p></div> <Badge variant={t.status === "completed" ? "default" : "secondary"} className="text-xs">{t.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
