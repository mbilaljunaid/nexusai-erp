import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Video } from "lucide-react"; export default function VirtualClassroom() { const { data: sessions = [], isLoading } = useQuery({ queryKey: ["/api/ed-virtual"],  }); const live = sessions.filter((s: any) => s.status === "live").length; const recorded = sessions.filter((s: any) => s.hasRecording).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Video className="h-8 w-8" /> Online Classroom & Virtual Learning </h1> <p className="text-muted-foreground mt-2">Live sessions, breakout rooms, chat, polls, recordings, playback, LMS integration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Sessions</p><p className="text-2xl font-bold">{sessions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Live</p><p className="text-2xl font-bold text-green-600">{live}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recorded</p><p className="text-2xl font-bold text-blue-600">{recorded}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Recording %</p><p className="text-2xl font-bold">{sessions.length > 0 ? ((recorded / sessions.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Sessions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : sessions.length === 0 ? <p className="text-muted-foreground text-center py-4">No sessions</p> : sessions.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`session-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.sessionId}</p><p className="text-xs text-muted-foreground">{s.attendees} attendees</p></div> <Badge variant={s.status === "live" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
