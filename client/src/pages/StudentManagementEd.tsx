import { useState } from "react"; import { useQuery, useMutation } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Badge } from "@/components/ui/badge"; import { Users, Plus, Trash2 } from "lucide-react"; import { queryClient } from "@/lib/queryClient"; import { useToast } from "@/hooks/use-toast"; export default function StudentManagementEd() { const { toast } = useToast(); const [newStud, setNewStud] = useState({ studentId: "", name: "", email: "", programId: "", status: "active" }); const { data: students = [], isLoading } = useQuery({ queryKey: ["/api/ed-students"], queryFn: () => fetch("/api/ed-students").then(r => r.json()).catch(() => []), }); const createMutation = useMutation({ mutationFn: (data: any) => fetch("/api/ed-students", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }).then(r => r.json()), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/ed-students"] }); setNewStud({ studentId: "", name: "", email: "", programId: "", status: "active" }); toast({ title: "Student added" }); }, }); const deleteMutation = useMutation({ mutationFn: (id: string) => fetch(`/api/ed-students/${id}`, { method: "DELETE" }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/ed-students"] }); toast({ title: "Student deleted" }); }, }); const active = students.filter((s: any) => s.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Users className="h-8 w-8" /> Student & Faculty Management </h1> <p className="text-muted-foreground mt-2">Demographics, enrollment, programs, departments, qualifications, progress tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Students</p><p className="text-2xl font-bold">{students.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{students.filter((s: any) => s.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{students.length > 0 ? ((active / students.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card data-testid="card-new-student"> <CardHeader><CardTitle className="text-base">Add Student</CardTitle></CardHeader> <CardContent className="space-y-3"> <div className="grid grid-cols-5 gap-2"> <Input placeholder="ID" value={newStud.studentId} onChange={(e) => setNewStud({ ...newStud, studentId: e.target.value })} data-testid="input-sid" className="text-sm" /> <Input placeholder="Name" value={newStud.name} onChange={(e) => setNewStud({ ...newStud, name: e.target.value })} data-testid="input-name" className="text-sm" /> <Input placeholder="Email" value={newStud.email} onChange={(e) => setNewStud({ ...newStud, email: e.target.value })} data-testid="input-email" className="text-sm" /> <Input placeholder="Program" value={newStud.programId} onChange={(e) => setNewStud({ ...newStud, programId: e.target.value })} data-testid="input-prog" className="text-sm" /> <Button onClick={() => createMutation.mutate(newStud)} disabled={createMutation.isPending || !newStud.studentId} size="sm" data-testid="button-add"> <Plus className="w-3 h-3" /> </Button> </div> </CardContent> </Card> <Card> <CardHeader><CardTitle className="text-base">Students</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : students.length === 0 ? <p className="text-muted-foreground text-center py-4">No students</p> : students.map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`student-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.studentId}</p><p className="text-xs text-muted-foreground">{s.email}</p></div> <div className="flex gap-2 items-center"> <Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> <Button size="icon" variant="ghost" onClick={() => deleteMutation.mutate(s.id)} data-testid={`btn-del-${s.id}`} className="h-7 w-7"> <Trash2 className="w-3 h-3" /> </Button> </div> </div> ))} </CardContent> </Card> </div> ); }
