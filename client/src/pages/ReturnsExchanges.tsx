import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { RefreshCw } from "lucide-react"; export default function ReturnsExchanges() { const { data: rmas = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/fashion-returns"], queryFn: () => fetch("/api/fashion-returns").then(r => r.json()).catch(() => []), }); const resellable = rmas.filter((r: any) => r.disposition === "resell").length; const avgReturnRate = rmas.length > 0 ? (rmas.reduce((sum: number, r: any) => sum + (parseFloat(r.returnRate) || 0), 0) / rmas.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <RefreshCw className="h-8 w-8" /> Returns, Fit & Exchange Management </h1> <p className="text-muted-foreground mt-2">RMA, fit feedback, size-swap, QC disposition, return % by SKU/size, analytics & trend detection</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">RMAs</p><p className="text-2xl font-bold">{rmas.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resellable</p><p className="text-2xl font-bold text-green-600">{resellable}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Return Rate</p><p className="text-2xl font-bold text-orange-600">{avgReturnRate}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Reasons Tracked</p><p className="text-2xl font-bold">{new Set(rmas.map((r: any) => r.reason)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Returns</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : rmas.length === 0 ? <p className="text-muted-foreground text-center py-4">No returns</p> : rmas.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`rma-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.rmaId}</p><p className="text-xs text-muted-foreground">Reason: {r.reason}</p></div> <Badge variant={r.disposition === "resell" ? "default" : "secondary"} className="text-xs">{r.disposition}</Badge> </div> ))} </CardContent> </Card> </div> ); }
