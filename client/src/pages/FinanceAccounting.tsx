import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function FinanceAccounting() { const { data: financials = [], isLoading } = useQuery({ queryKey: ["/api/telecom-finance"],  }); const totalRev = financials.reduce((sum: number, f: any) => sum + (parseFloat(f.revenue) || 0), 0); const totalCost = financials.reduce((sum: number, f: any) => sum + (parseFloat(f.cost) || 0), 0); const profit = totalRev - totalCost; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BarChart3 className="h-8 w-8" /> Finance, Accounting & EPM </h1> <p className="text-muted-foreground mt-2">GL/AR/AP, revenue recognition, cost tracking, profitability by plan, forecast vs actual, variance</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Revenue</p><p className="text-2xl font-bold text-green-600">${(totalRev / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Cost</p><p className="text-2xl font-bold text-red-600">${(totalCost / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Profit</p><p className="text-2xl font-bold text-blue-600">${(profit / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Margin %</p><p className="text-2xl font-bold">{totalRev > 0 ? ((profit / totalRev) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Financial Summary</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : financials.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : financials.slice(0, 10).map((f: any) => ( <div key={f.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`fin-${f.id}`}> <div className="flex-1"><p className="font-semibold">{f.entityId}</p><p className="text-xs text-muted-foreground">Rev: ${f.revenue} • Cost: ${f.cost}</p></div> <Badge variant={parseFloat(f.profit) > 0 ? "default" : "secondary"} className="text-xs">${f.profit}</Badge> </div> ))} </CardContent> </Card> </div> ); }
