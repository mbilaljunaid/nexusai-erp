import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { FileText } from "lucide-react"; export default function BillingCustomers() { const { data: customers = [], isLoading } = useQuery({ queryKey: ["/api/energy-billing"], queryFn: () => fetch("/api/energy-billing").then(r => r.json()).catch(() => []), }); const active = customers.filter((c: any) => c.status === "active").length; const totalUsage = customers.reduce((sum: number, c: any) => sum + (parseFloat(c.usage) || 0), 0); return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><FileText className="h-8 w-8" /> Billing & Customer Management</h1><p className="text-muted-foreground mt-2">Meters, usage, billing, tariffs, complaints, support</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Customers</p><p className="text-2xl font-bold">{customers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Usage</p><p className="text-2xl font-bold text-blue-600">{(totalUsage / 1000).toFixed(0)}K MWh</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{customers.length > 0 ? ((active / customers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Customers</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : customers.length === 0 ? <p className="text-muted-foreground text-center py-4">No customers</p> : customers.slice(0, 10).map((c: any) => (<div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`cust-${c.id}`}><div className="flex-1"><p className="font-semibold">{c.customerId}</p><p className="text-xs text-muted-foreground">{c.usage} MWh</p></div><Badge variant={c.status === "active" ? "default" : "secondary"} className="text-xs">{c.status}</Badge></div>))}</CardContent></Card> </div> ); }
