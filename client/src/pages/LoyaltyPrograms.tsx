import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Gift } from "lucide-react"; export default function LoyaltyPrograms() { const { data: loyalty = [], isLoading } = useQuery({ queryKey: ["/api/telecom-loyalty"], queryFn: () => fetch("/api/telecom-loyalty").then(r => r.json()).catch(() => []), }); const totalPoints = loyalty.reduce((sum: number, l: any) => sum + (parseInt(l.points) || 0), 0); const gold = loyalty.filter((l: any) => l.tier === "gold").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Gift className="h-8 w-8" /> Loyalty Programs & Rewards </h1> <p className="text-muted-foreground mt-2">Customer tiers, loyalty points, rewards redemption, campaign targeting, up-sell/cross-sell</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Members</p><p className="text-2xl font-bold">{loyalty.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Points</p><p className="text-2xl font-bold text-green-600">{(totalPoints / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Gold Members</p><p className="text-2xl font-bold text-blue-600">{gold}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Points</p><p className="text-2xl font-bold">{loyalty.length > 0 ? (totalPoints / loyalty.length).toFixed(0) : 0}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Members</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : loyalty.length === 0 ? <p className="text-muted-foreground text-center py-4">No members</p> : loyalty.slice(0, 10).map((l: any) => ( <div key={l.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`loyalty-${l.id}`}> <div className="flex-1"><p className="font-semibold">{l.customerId}</p><p className="text-xs text-muted-foreground">Points: {l.points}</p></div> <Badge variant={l.tier === "gold" ? "default" : "secondary"} className="text-xs">{l.tier}</Badge> </div> ))} </CardContent> </Card> </div> ); }
