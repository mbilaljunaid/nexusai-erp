import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CheckSquare } from "lucide-react"; export default function ComplianceReporting() { const { data: regulations = [], isLoading } = useQuery({ queryKey: ["/api/gov-compliance"],  }); const compliant = regulations.filter((r: any) => r.status === "compliant").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><CheckSquare className="h-8 w-8" /> Compliance & Reporting</h1><p className="text-muted-foreground mt-2">Regulations, audits, reporting, exceptions</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Regulations</p><p className="text-2xl font-bold">{regulations.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliant</p><p className="text-2xl font-bold text-green-600">{compliant}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">At Risk</p><p className="text-2xl font-bold text-yellow-600">{regulations.filter((r: any) => r.status === "at-risk").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Compliance %</p><p className="text-2xl font-bold">{regulations.length > 0 ? ((compliant / regulations.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Regulations</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : regulations.length === 0 ? <p className="text-muted-foreground text-center py-4">No regulations</p> : regulations.slice(0, 10).map((r: any) => (<div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`reg-${r.id}`}><div className="flex-1"><p className="font-semibold">{r.regulationId}</p><p className="text-xs text-muted-foreground">{r.departmentId}</p></div><Badge variant={r.status === "compliant" ? "default" : "destructive"} className="text-xs">{r.status}</Badge></div>))}</CardContent></Card> </div> ); }
