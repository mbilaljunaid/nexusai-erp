import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Sparkles } from "lucide-react"; export default function PredictiveMaintenance() { const { data: predictions = [], isLoading } = useQuery({ queryKey: ["/api/mfg-predictive"],  }); const highRisk = predictions.filter((p: any) => parseFloat(p.riskScore) > 70).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Sparkles className="h-8 w-8" /> AI & Predictive Analytics </h1> <p className="text-muted-foreground mt-2">Predictive maintenance, yield optimization, demand forecasting, replenishment, quality prediction</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Models</p><p className="text-2xl font-bold">{predictions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">High Risk</p><p className="text-2xl font-bold text-red-600">{highRisk}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Score</p><p className="text-2xl font-bold text-blue-600">{predictions.length > 0 ? (predictions.reduce((sum: number, p: any) => sum + (parseFloat(p.riskScore) || 0), 0) / predictions.length).toFixed(0) : 0}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Risk %</p><p className="text-2xl font-bold">{predictions.length > 0 ? ((highRisk / predictions.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Predictions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : predictions.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : predictions.slice(0, 10).map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`pred-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.assetId}</p><p className="text-xs text-muted-foreground">Score: {p.riskScore}%</p></div> <Badge variant={parseFloat(p.riskScore) > 70 ? "destructive" : "default"} className="text-xs">{p.riskScore}%</Badge> </div> ))} </CardContent> </Card> </div> ); }
