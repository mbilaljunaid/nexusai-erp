import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CreditCard } from "lucide-react"; export default function BillingEducation() { const { data: invoices = [], isLoading } = useQuery({ queryKey: ["/api/ed-billing"], queryFn: () => fetch("/api/ed-billing").then(r => r.json()).catch(() => []), }); const paid = invoices.filter((i: any) => i.status === "paid").length; const total = invoices.reduce((sum: number, i: any) => sum + (parseFloat(i.amount) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CreditCard className="h-8 w-8" /> Billing, Payments & Finance </h1> <p className="text-muted-foreground mt-2">Fee structure, tuition, payments, scholarships, refunds, invoices, finance GL integration</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Invoices</p><p className="text-2xl font-bold">{invoices.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Paid</p><p className="text-2xl font-bold text-green-600">{paid}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total</p><p className="text-2xl font-bold text-blue-600">${(total / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Collection %</p><p className="text-2xl font-bold">{invoices.length > 0 ? ((paid / invoices.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Invoices</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : invoices.length === 0 ? <p className="text-muted-foreground text-center py-4">No invoices</p> : invoices.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`inv-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.invoiceId}</p><p className="text-xs text-muted-foreground">${i.amount}</p></div> <Badge variant={i.status === "paid" ? "default" : "secondary"} className="text-xs">{i.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
