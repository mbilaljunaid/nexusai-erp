import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Navigation } from "lucide-react"; export default function RouteOptimization() { const { data: routes = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/logistics-routes"], queryFn: () => fetch("/api/logistics-routes").then(r => r.json()).catch(() => []), }); const completed = routes.filter((r: any) => r.status === "completed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Navigation className="h-8 w-8" /> Route Optimization </h1> <p className="text-muted-foreground mt-2">Planning, scheduling, traffic, dynamic rerouting, priority management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Routes</p><p className="text-2xl font-bold">{routes.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-blue-600">{routes.filter((r: any) => r.status === "active").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{routes.length > 0 ? ((completed / routes.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Routes</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : routes.length === 0 ? <p className="text-muted-foreground text-center py-4">No routes</p> : routes.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`route-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.routeId}</p><p className="text-xs text-muted-foreground">{r.stops} stops</p></div> <Badge variant={r.status === "completed" ? "default" : "secondary"} className="text-xs">{r.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
