import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { FileCheck } from "lucide-react"; export default function RegulatoryeCTD() { const { data: submissions = [], isLoading } = useQuery({ queryKey: ["/api/pharma-regulatory"], queryFn: () => fetch("/api/pharma-regulatory").then(r => r.json()).catch(() => []), }); const approved = submissions.filter((s: any) => s.status === "approved").length; const inReview = submissions.filter((s: any) => s.status === "under-review").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <FileCheck className="h-8 w-8" /> Regulatory Submissions & eCTD </h1> <p className="text-muted-foreground mt-2">eCTD dossier builder, module mapping, submission tracking, commitments, correspondence management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Submissions</p><p className="text-2xl font-bold">{submissions.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Under Review</p><p className="text-2xl font-bold text-blue-600">{inReview}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approval %</p><p className="text-2xl font-bold">{submissions.length > 0 ? ((approved / submissions.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Submissions</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : submissions.length === 0 ? <p className="text-muted-foreground text-center py-4">No submissions</p> : submissions.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`submission-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.submissionId}</p><p className="text-xs text-muted-foreground">Region: {s.region} • Product: {s.productId}</p></div> <Badge variant={s.status === "approved" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
