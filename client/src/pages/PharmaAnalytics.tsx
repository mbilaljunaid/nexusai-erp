import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { BarChart3 } from "lucide-react"; export default function PharmaAnalytics() { const { data: analytics = [], isLoading } = useQuery({ queryKey: ["/api/pharma-analytics"],  }); const avgYield = analytics.length > 0 ? (analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.yield) || 0), 0) / analytics.length).toFixed(1) : 0; const avgCost = analytics.length > 0 ? (analytics.reduce((sum: number, a: any) => sum + (parseFloat(a.costPerUnit) || 0), 0) / analytics.length).toFixed(2) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <BarChart3 className="h-8 w-8" /> Pharma BI & Analytics </h1> <p className="text-muted-foreground mt-2">Batch yield analysis, cost per unit, quality trends, regulatory submissions, supplier metrics, KPIs</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"> <CardContent className="pt-0"> <p className="text-xs text-muted-foreground">Batches Analyzed</p> <p className="text-2xl font-bold">{analytics.length}</p> </CardContent> </Card> <Card className="p-3"> <CardContent className="pt-0"> <p className="text-xs text-muted-foreground">Avg Yield %</p> <p className="text-2xl font-bold text-green-600">{avgYield}%</p> </CardContent> </Card> <Card className="p-3"> <CardContent className="pt-0"> <p className="text-xs text-muted-foreground">Avg Cost/Unit</p> <p className="text-2xl font-bold">${avgCost}</p> </CardContent> </Card> <Card className="p-3"> <CardContent className="pt-0"> <p className="text-xs text-muted-foreground">Products</p> <p className="text-2xl font-bold">{new Set(analytics.map((a: any) => a.productId)).size}</p> </CardContent> </Card> </div> <Card> <CardHeader><CardTitle className="text-base">Key Metrics</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : analytics.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : analytics.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate" data-testid={`metric-${a.id}`}> <p className="font-semibold">{a.productId}</p> <p className="text-xs text-muted-foreground">Yield: {a.yield}% • Cost: ${a.costPerUnit} • Batches: {a.batchCount}</p> </div> ))} </CardContent> </Card> </div> ); }
