import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Box } from "lucide-react"; export default function InventoryWarehousingCPG() { const { data: inventory = [], isLoading } = useQuery({ queryKey: ["/api/cpg-inventory"], queryFn: () => fetch("/api/cpg-inventory").then(r => r.json()).catch(() => []), }); const totalQty = inventory.reduce((sum: number, i: any) => sum + (parseInt(i.qty) || 0), 0); const expired = inventory.filter((i: any) => new Date(i.expiryDate) < new Date()).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Box className="h-8 w-8" /> Inventory & Warehouse Management </h1> <p className="text-muted-foreground mt-2">Batch-level tracking, FIFO/FEFO, cold storage, GRN, pick/pack/ship, FEFO allocation, recall management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Units</p><p className="text-2xl font-bold">{totalQty}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SKUs</p><p className="text-2xl font-bold">{inventory.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Expired</p><p className="text-2xl font-bold text-red-600">{expired}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Stock</p><p className="text-2xl font-bold">{inventory.length > 0 ? (totalQty / inventory.length).toFixed(0) : 0}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Inventory Locations</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : inventory.length === 0 ? <p className="text-muted-foreground text-center py-4">No inventory</p> : inventory.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`inv-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.sku}</p><p className="text-xs text-muted-foreground">Batch: {i.batchId} • Expiry: {i.expiryDate}</p></div> <Badge variant={parseInt(i.qty) > 0 ? "default" : "destructive"} className="text-xs">{i.qty}</Badge> </div> ))} </CardContent> </Card> </div> ); }
