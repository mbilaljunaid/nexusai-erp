import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { MapPin } from "lucide-react"; export default function TravelItinerary() { const { data: itineraries = [], isLoading } = useQuery({ queryKey: ["/api/hospitality-itinerary"], queryFn: () => fetch("/api/hospitality-itinerary").then(r => r.json()).catch(() => []), }); const confirmed = itineraries.filter((i: any) => i.status === "confirmed").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><MapPin className="h-8 w-8" /> Travel Itinerary & Ticketing</h1><p className="text-muted-foreground mt-2">Itineraries, flights, transport, packages</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Itineraries</p><p className="text-2xl font-bold">{itineraries.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Confirmed</p><p className="text-2xl font-bold text-green-600">{confirmed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{itineraries.filter((i: any) => i.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Confirmation %</p><p className="text-2xl font-bold">{itineraries.length > 0 ? ((confirmed / itineraries.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Itineraries</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : itineraries.length === 0 ? <p className="text-muted-foreground text-center py-4">No itineraries</p> : itineraries.slice(0, 10).map((i: any) => (<div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`itin-${i.id}`}><div className="flex-1"><p className="font-semibold">{i.itineraryId}</p><p className="text-xs text-muted-foreground">{i.days} days</p></div><Badge variant={i.status === "confirmed" ? "default" : "secondary"} className="text-xs">{i.status}</Badge></div>))}</CardContent></Card> </div> ); }
