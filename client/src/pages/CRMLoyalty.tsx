import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Heart } from "lucide-react"; export default function CRMLoyalty() { const { data: customers = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/cpg-crm"], queryFn: () => fetch("/api/cpg-crm").then(r => r.json()).catch(() => []), }); const totalPoints = customers.reduce((sum: number, c: any) => sum + (parseInt(c.points) || 0), 0); const premium = customers.filter((c: any) => c.tier === "premium").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Heart className="h-8 w-8" /> CRM & Loyalty Programs </h1> <p className="text-muted-foreground mt-2">Customer segmentation, tiering, loyalty points, campaign execution, feedback management, interactions</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Customers</p><p className="text-2xl font-bold">{customers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Premium</p><p className="text-2xl font-bold text-green-600">{premium}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Points</p><p className="text-2xl font-bold">{(totalPoints / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Points</p><p className="text-2xl font-bold">{customers.length > 0 ? (totalPoints / customers.length).toFixed(0) : 0}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Customers</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : customers.length === 0 ? <p className="text-muted-foreground text-center py-4">No customers</p> : customers.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`customer-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.customerId}</p><p className="text-xs text-muted-foreground">Points: {c.points}</p></div> <Badge variant={c.tier === "premium" ? "default" : "secondary"} className="text-xs">{c.tier}</Badge> </div> ))} </CardContent> </Card> </div> ); }
