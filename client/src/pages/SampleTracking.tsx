import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Package } from "lucide-react"; export default function SampleTracking() { const { data: samples = [], isLoading } = useQuery({ queryKey: ["/api/fashion-samples"],  }); const approved = samples.filter((s: any) => s.status === "approved").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Package className="h-8 w-8" /> Sample Request & Tracking </h1> <p className="text-muted-foreground mt-2">Proto, fit samples, size sets, fit sessions, approval workflow, photo evidence, PPAP style pre-production</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Samples</p><p className="text-2xl font-bold">{samples.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approved</p><p className="text-2xl font-bold text-green-600">{approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{samples.length - approved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Approval %</p><p className="text-2xl font-bold">{samples.length > 0 ? ((approved / samples.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Samples</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : samples.length === 0 ? <p className="text-muted-foreground text-center py-4">No samples</p> : samples.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`sample-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.sampleId}</p><p className="text-xs text-muted-foreground">Type: {s.type}</p></div> <Badge variant={s.status === "approved" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
