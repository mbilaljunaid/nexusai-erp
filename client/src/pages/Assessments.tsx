import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CheckSquare } from "lucide-react"; export default function Assessments() { const { data: assessments = [], isLoading } = useQuery({ queryKey: ["/api/ed-assessments"], queryFn: () => fetch("/api/ed-assessments").then(r => r.json()).catch(() => []), }); const completed = assessments.filter((a: any) => a.status === "completed").length; const avgScore = assessments.length > 0 ? (assessments.reduce((sum: number, a: any) => sum + (parseFloat(a.score) || 0), 0) / assessments.length).toFixed(0) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CheckSquare className="h-8 w-8" /> Assessments, Grading & Certification </h1> <p className="text-muted-foreground mt-2">Quizzes, exams, grading rubrics, grade books, certificates, transcripts, reassessments</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total</p><p className="text-2xl font-bold">{assessments.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Score</p><p className="text-2xl font-bold text-blue-600">{avgScore}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pass %</p><p className="text-2xl font-bold">{assessments.length > 0 ? ((completed / assessments.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Assessments</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : assessments.length === 0 ? <p className="text-muted-foreground text-center py-4">No assessments</p> : assessments.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`assess-${a.id}`}> <div className="flex-1"><p className="font-semibold">{a.assessmentId}</p><p className="text-xs text-muted-foreground">Score: {a.score}%</p></div> <Badge variant={a.status === "completed" ? "default" : "secondary"} className="text-xs">{a.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
