import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { CreditCard } from "lucide-react"; export default function BillingInsurance() { const { data: claims = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/hc-billing"], queryFn: () => fetch("/api/hc-billing").then(r => r.json()).catch(() => []), }); const processed = claims.filter((c: any) => c.status === "processed").length; const totalBilled = claims.reduce((sum: number, c: any) => sum + (parseFloat(c.amountBilled) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <CreditCard className="h-8 w-8" /> Billing, Insurance & Revenue Cycle </h1> <p className="text-muted-foreground mt-2">Insurance eligibility, claim submission, EOB/ERA, patient billing, co-pay, reconciliation</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Claims</p><p className="text-2xl font-bold">{claims.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Processed</p><p className="text-2xl font-bold text-green-600">{processed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Billed</p><p className="text-2xl font-bold text-blue-600">${(totalBilled / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Process %</p><p className="text-2xl font-bold">{claims.length > 0 ? ((processed / claims.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Claims</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : claims.length === 0 ? <p className="text-muted-foreground text-center py-4">No claims</p> : claims.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`claim-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.claimId}</p><p className="text-xs text-muted-foreground">${c.amountBilled}</p></div> <Badge variant={c.status === "processed" ? "default" : "secondary"} className="text-xs">{c.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
