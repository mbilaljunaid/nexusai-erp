import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Sparkles } from "lucide-react"; export default function PersonalizedLearning() { const { data: learning = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/ed-personalized"], queryFn: () => fetch("/api/ed-personalized").then(r => r.json()).catch(() => []), }); const atRisk = learning.filter((l: any) => l.riskStatus === "at-risk").length; const onTrack = learning.filter((l: any) => l.progressStatus === "on-track").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Sparkles className="h-8 w-8" /> AI & Personalized Learning </h1> <p className="text-muted-foreground mt-2">Adaptive learning paths, content recommendations, early warning, predictive performance analytics</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Students</p><p className="text-2xl font-bold">{learning.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">On Track</p><p className="text-2xl font-bold text-green-600">{onTrack}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">At Risk</p><p className="text-2xl font-bold text-red-600">{atRisk}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Success %</p><p className="text-2xl font-bold">{learning.length > 0 ? ((onTrack / learning.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Learning Paths</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : learning.length === 0 ? <p className="text-muted-foreground text-center py-4">No data</p> : learning.slice(0, 10).map((l: any) => ( <div key={l.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`learn-${l.id}`}> <div className="flex-1"><p className="font-semibold">{l.studentId}</p><p className="text-xs text-muted-foreground">Progress: {l.progressScore}%</p></div> <Badge variant={l.progressStatus === "on-track" ? "default" : "secondary"} className="text-xs">{l.progressStatus}</Badge> </div> ))} </CardContent> </Card> </div> ); }
