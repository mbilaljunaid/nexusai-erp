import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { DollarSign } from "lucide-react"; export default function CostingMarginCPG() { const { data: costing = [], isLoading } = useQuery({ queryKey: ["/api/cpg-costing"],  }); const avgMargin = costing.length > 0 ? (costing.reduce((sum: number, c: any) => sum + (parseFloat(c.margin) || 0), 0) / costing.length).toFixed(1) : 0; const totalRevenue = costing.reduce((sum: number, c: any) => sum + (parseFloat(c.msrp) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <DollarSign className="h-8 w-8" /> Costing, Margin & Profitability </h1> <p className="text-muted-foreground mt-2">Full cost build, landed cost, COGS, P&L by SKU, margin tracking, promotion impact, break-even</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">SKUs Costed</p><p className="text-2xl font-bold">{costing.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Margin %</p><p className="text-2xl font-bold text-green-600">{avgMargin}%</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Revenue</p><p className="text-2xl font-bold">${(totalRevenue / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Collections</p><p className="text-2xl font-bold">{new Set(costing.map((c: any) => c.collection)).size}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">P&L by SKU</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : costing.length === 0 ? <p className="text-muted-foreground text-center py-4">No costing</p> : costing.slice(0, 10).map((c: any) => ( <div key={c.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`costing-${c.id}`}> <div className="flex-1"><p className="font-semibold">{c.sku}</p><p className="text-xs text-muted-foreground">Cost: ${c.cost} • MSRP: ${c.msrp}</p></div> <Badge variant={c.margin > 30 ? "default" : "secondary"} className="text-xs">{c.margin}% margin</Badge> </div> ))} </CardContent> </Card> </div> ); }
