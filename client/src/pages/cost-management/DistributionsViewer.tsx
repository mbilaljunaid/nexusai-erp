import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Search, Download, Filter } from 'lucide-react';

export default function DistributionsViewer() {
    const distributions = [
        { id: '1', date: '2026-01-12', doc: 'RCT-1004', type: 'Receiving', account: '01-000-1410-0000', dr: 500.00, cr: 0.00 },
        { id: '2', date: '2026-01-12', doc: 'RCT-1004', type: 'Accrual', account: '01-000-2210-0000', dr: 0.00, cr: 500.00 },
        { id: '3', date: '2026-01-13', doc: 'WO-1001', type: 'Material Issue', account: '01-000-1410-0000', dr: 0.00, cr: 10.00 },
        { id: '4', date: '2026-01-13', doc: 'WO-1001', type: 'WIP Charge', account: '01-000-1510-0000', dr: 10.00, cr: 0.00 },
    ];

    return (
        <div className="space-y-6">
            <div className="flex flex-col gap-2">
                <h1 className="text-3xl font-bold tracking-tight">Cost Distributions</h1>
                <p className="text-muted-foreground">Audit trail of all accounting entries generated by Cost Processor.</p>
            </div>

            <Card>
                <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
                    <CardTitle className="text-lg font-medium">Accounting Entries</CardTitle>
                    <div className="flex space-x-2">
                        <Button variant="outline" size="sm"><Download className="mr-2 h-4 w-4" /> Export</Button>
                    </div>
                </CardHeader>
                <CardContent>
                    <div className="flex items-center space-x-2 mb-4">
                        <Input placeholder="Search document number..." className="max-w-xs" />
                        <Button variant="ghost"><Search className="h-4 w-4" /></Button>
                        <Button variant="ghost"><Filter className="h-4 w-4" /></Button>
                    </div>

                    <Table>
                        <TableHeader>
                            <TableRow>
                                <TableHead>Date</TableHead>
                                <TableHead>Document</TableHead>
                                <TableHead>Transaction Type</TableHead>
                                <TableHead>GL Account</TableHead>
                                <TableHead className="text-right">Debit</TableHead>
                                <TableHead className="text-right">Credit</TableHead>
                            </TableRow>
                        </TableHeader>
                        <TableBody>
                            {distributions.map((dist) => (
                                <TableRow key={dist.id}>
                                    <TableCell>{dist.date}</TableCell>
                                    <TableCell>{dist.doc}</TableCell>
                                    <TableCell>{dist.type}</TableCell>
                                    <TableCell className="font-mono text-xs">{dist.account}</TableCell>
                                    <TableCell className="text-right">{dist.dr > 0 ? dist.dr.toFixed(2) : '-'}</TableCell>
                                    <TableCell className="text-right">{dist.cr > 0 ? dist.cr.toFixed(2) : '-'}</TableCell>
                                </TableRow>
                            ))}
                        </TableBody>
                    </Table>
                </CardContent>
            </Card>
        </div>
    );
}
