import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Building2 } from "lucide-react"; export default function Warehouse() { const { data: warehouse = [], isLoading } = useQuery({ queryKey: ["/api/retail-warehouse"],  }); const active = warehouse.filter((w: any) => w.status === "active").length; const totalCapacity = warehouse.reduce((sum: number, w: any) => sum + (parseInt(w.capacity) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Building2 className="h-8 w-8" /> Warehouse & Fulfillment </h1> <p className="text-muted-foreground mt-2">Masters, zones, pick/pack/ship, returns, reverse logistics, multi-warehouse allocation</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Warehouses</p><p className="text-2xl font-bold">{warehouse.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Capacity</p><p className="text-2xl font-bold text-blue-600">{totalCapacity}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{warehouse.length > 0 ? ((active / warehouse.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Warehouses</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : warehouse.length === 0 ? <p className="text-muted-foreground text-center py-4">No warehouses</p> : warehouse.slice(0, 10).map((w: any) => ( <div key={w.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`wh-${w.id}`}> <div className="flex-1"><p className="font-semibold">{w.warehouseId}</p><p className="text-xs text-muted-foreground">Cap: {w.capacity}</p></div> <Badge variant={w.status === "active" ? "default" : "secondary"} className="text-xs">{w.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
