import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Users } from "lucide-react"; export default function SubscriberAccounts() { const { data: subscribers = [], isLoading } = useQuery<any[]>({ queryKey: ["/api/telecom-subscribers"], queryFn: () => fetch("/api/telecom-subscribers").then(r => r.json()).catch(() => []), }); const active = subscribers.filter((s: any) => s.status === "active").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Users className="h-8 w-8" /> Subscriber & Account Management</h1><p className="text-muted-foreground mt-2">Profiles, plans, subscriptions, usage tracking</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Subscribers</p><p className="text-2xl font-bold">{subscribers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Inactive</p><p className="text-2xl font-bold text-yellow-600">{subscribers.filter((s: any) => s.status === "inactive").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{subscribers.length > 0 ? ((active / subscribers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Subscribers</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : subscribers.length === 0 ? <p className="text-muted-foreground text-center py-4">No subscribers</p> : subscribers.slice(0, 10).map((s: any) => (<div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`sub-${s.id}`}><div className="flex-1"><p className="font-semibold">{s.subscriberId}</p><p className="text-xs text-muted-foreground">{s.plan}</p></div><Badge variant={s.status === "active" ? "default" : "secondary"} className="text-xs">{s.status}</Badge></div>))}</CardContent></Card> </div> ); }
