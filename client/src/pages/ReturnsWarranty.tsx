import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { RefreshCw } from "lucide-react"; export default function ReturnsWarranty() { const { data: rmas = [], isLoading } = useQuery({ queryKey: ["/api/cpg-returns"], queryFn: () => fetch("/api/cpg-returns").then(r => r.json()).catch(() => []), }); const processed = rmas.filter((r: any) => r.status === "processed").length; const totalRefund = rmas.reduce((sum: number, r: any) => sum + (parseFloat(r.refund) || 0), 0); return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <RefreshCw className="h-8 w-8" /> Returns & Warranty Management </h1> <p className="text-muted-foreground mt-2">RMA capture, reason codes, warranty claims, reverse logistics, inspection, refund/replacement processing</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">RMAs</p><p className="text-2xl font-bold">{rmas.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Processed</p><p className="text-2xl font-bold text-green-600">{processed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Refunds</p><p className="text-2xl font-bold text-red-600">${totalRefund.toFixed(0)}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Processing %</p><p className="text-2xl font-bold">{rmas.length > 0 ? ((processed / rmas.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Returns</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : rmas.length === 0 ? <p className="text-muted-foreground text-center py-4">No returns</p> : rmas.slice(0, 10).map((r: any) => ( <div key={r.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`rma-${r.id}`}> <div className="flex-1"><p className="font-semibold">{r.rmaId}</p><p className="text-xs text-muted-foreground">Reason: {r.reason}</p></div> <Badge variant={r.status === "processed" ? "default" : "secondary"} className="text-xs">${r.refund}</Badge> </div> ))} </CardContent> </Card> </div> ); }
