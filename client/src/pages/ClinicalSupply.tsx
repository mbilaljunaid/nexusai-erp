import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Vial } from "lucide-react"; export default function ClinicalSupply() { const { data: supplies = [], isLoading } = useQuery({ queryKey: ["/api/pharma-clinical"], queryFn: () => fetch("/api/pharma-clinical").then(r => r.json()).catch(() => []), }); const allocated = supplies.filter((s: any) => s.status === "allocated").length; const returned = supplies.filter((s: any) => s.status === "returned").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Vial className="h-8 w-8" /> Clinical Supply Management (CTMS/EDC) </h1> <p className="text-muted-foreground mt-2">IMP kit assignment, randomization, site logistics, expiry tracking, reconciliation, safety reporting</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Kits</p><p className="text-2xl font-bold">{supplies.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Allocated</p><p className="text-2xl font-bold text-blue-600">{allocated}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Returned</p><p className="text-2xl font-bold text-green-600">{returned}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Return %</p><p className="text-2xl font-bold">{supplies.length > 0 ? ((returned / supplies.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Supply Kits</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : supplies.length === 0 ? <p className="text-muted-foreground text-center py-4">No kits</p> : supplies.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`supply-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.kitId}</p><p className="text-xs text-muted-foreground">Study: {s.studyId} • Site: {s.siteId}</p></div> <Badge variant={s.status === "allocated" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
