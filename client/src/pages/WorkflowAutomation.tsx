import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Zap } from "lucide-react"; export default function WorkflowAutomation() { const { data: workflows = [], isLoading } = useQuery({ queryKey: ["/api/gov-workflows"], queryFn: () => fetch("/api/gov-workflows").then(r => r.json()).catch(() => []), }); const active = workflows.filter((w: any) => w.status === "active").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Zap className="h-8 w-8" /> Workflow Automation</h1><p className="text-muted-foreground mt-2">Approvals, task assignment, SLA, escalation</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Workflows</p><p className="text-2xl font-bold">{workflows.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-blue-600">{workflows.filter((w: any) => w.status === "completed").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active %</p><p className="text-2xl font-bold">{workflows.length > 0 ? ((active / workflows.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Workflows</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : workflows.length === 0 ? <p className="text-muted-foreground text-center py-4">No workflows</p> : workflows.slice(0, 10).map((w: any) => (<div key={w.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`wf-${w.id}`}><div className="flex-1"><p className="font-semibold">{w.workflowId}</p><p className="text-xs text-muted-foreground">{w.stepId}</p></div><Badge variant={w.status === "active" ? "default" : "secondary"} className="text-xs">{w.status}</Badge></div>))}</CardContent></Card> </div> ); }
