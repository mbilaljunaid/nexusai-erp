import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Pill } from "lucide-react"; export default function PharmacyInventory() { const { data: meds = [], isLoading } = useQuery({ queryKey: ["/api/hc-pharmacy"],  }); const inStock = meds.filter((m: any) => m.stockQty > m.minQty).length; const lowStock = meds.filter((m: any) => m.stockQty <= m.minQty).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Pill className="h-8 w-8" /> Pharmacy & Inventory </h1> <p className="text-muted-foreground mt-2">Medication master, stock tracking, prescriptions, dispensing, replenishment, controlled substances</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Items</p><p className="text-2xl font-bold">{meds.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Stock</p><p className="text-2xl font-bold text-green-600">{inStock}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Low Stock</p><p className="text-2xl font-bold text-red-600">{lowStock}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Stock %</p><p className="text-2xl font-bold">{meds.length > 0 ? ((inStock / meds.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Medications</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : meds.length === 0 ? <p className="text-muted-foreground text-center py-4">No medications</p> : meds.slice(0, 10).map((m: any) => ( <div key={m.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`med-${m.id}`}> <div className="flex-1"><p className="font-semibold">{m.medicationId}</p><p className="text-xs text-muted-foreground">Stock: {m.stockQty} / Min: {m.minQty}</p></div> <Badge variant={m.stockQty > m.minQty ? "default" : "destructive"} className="text-xs">{m.stockQty}</Badge> </div> ))} </CardContent> </Card> </div> ); }
