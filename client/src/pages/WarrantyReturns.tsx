import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { RefreshCw } from "lucide-react"; export default function WarrantyReturns() { const { data: warranty = [], isLoading } = useQuery({ queryKey: ["/api/telecom-warranty"], queryFn: () => fetch("/api/telecom-warranty").then(r => r.json()).catch(() => []), }); const resolved = warranty.filter((w: any) => w.status === "resolved").length; const replacement = warranty.filter((w: any) => w.resolution === "replacement").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <RefreshCw className="h-8 w-8" /> Returns, Warranty & Device Replacement </h1> <p className="text-muted-foreground mt-2">Warranty claims, device replacement, reverse logistics, refunds, root cause analysis</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Claims</p><p className="text-2xl font-bold">{warranty.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolved</p><p className="text-2xl font-bold text-green-600">{resolved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Replacements</p><p className="text-2xl font-bold text-blue-600">{replacement}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolution %</p><p className="text-2xl font-bold">{warranty.length > 0 ? ((resolved / warranty.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Warranty Claims</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : warranty.length === 0 ? <p className="text-muted-foreground text-center py-4">No claims</p> : warranty.slice(0, 10).map((w: any) => ( <div key={w.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`warranty-${w.id}`}> <div className="flex-1"><p className="font-semibold">{w.claimId}</p><p className="text-xs text-muted-foreground">{w.deviceId} • {w.resolution}</p></div> <Badge variant={w.status === "resolved" ? "default" : "secondary"} className="text-xs">{w.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
