import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Clock } from "lucide-react"; export default function Attendance() { const { data: attendance = [], isLoading } = useQuery({ queryKey: ["/api/ed-attendance"], queryFn: () => fetch("/api/ed-attendance").then(r => r.json()).catch(() => []), }); const present = attendance.filter((a: any) => a.status === "present").length; const absent = attendance.filter((a: any) => a.status === "absent").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Clock className="h-8 w-8" /> Attendance & Scheduling </h1> <p className="text-muted-foreground mt-2">Timetables, class scheduling, attendance tracking, faculty assignment, leave management</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Records</p><p className="text-2xl font-bold">{attendance.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Present</p><p className="text-2xl font-bold text-green-600">{present}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Absent</p><p className="text-2xl font-bold text-red-600">{absent}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Rate %</p><p className="text-2xl font-bold">{attendance.length > 0 ? ((present / attendance.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Attendance</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : attendance.length === 0 ? <p className="text-muted-foreground text-center py-4">No records</p> : attendance.slice(0, 10).map((a: any) => ( <div key={a.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`attend-${a.id}`}> <div className="flex-1"><p className="font-semibold">{a.classId}</p><p className="text-xs text-muted-foreground">{a.date}</p></div> <Badge variant={a.status === "present" ? "default" : "secondary"} className="text-xs">{a.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
