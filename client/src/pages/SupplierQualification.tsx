import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Building2 } from "lucide-react"; export default function SupplierQualification() { const { data: suppliers = [], isLoading } = useQuery({ queryKey: ["/api/pharma-suppliers"],  }); const qualified = suppliers.filter((s: any) => s.status === "qualified").length; const audited = suppliers.filter((s: any) => s.auditStatus === "passed").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Building2 className="h-8 w-8" /> Supplier Qualification & Management </h1> <p className="text-muted-foreground mt-2">CMO, contract labs, audits, certificates (COA, GMP), risk classification, performance tracking</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Suppliers</p><p className="text-2xl font-bold">{suppliers.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Qualified</p><p className="text-2xl font-bold text-green-600">{qualified}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Audited</p><p className="text-2xl font-bold text-blue-600">{audited}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Qualification %</p><p className="text-2xl font-bold">{suppliers.length > 0 ? ((qualified / suppliers.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Suppliers</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : suppliers.length === 0 ? <p className="text-muted-foreground text-center py-4">No suppliers</p> : suppliers.slice(0, 10).map((s: any) => ( <div key={s.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`supplier-${s.id}`}> <div className="flex-1"><p className="font-semibold">{s.supplierId}</p><p className="text-xs text-muted-foreground">Type: {s.type}</p></div> <Badge variant={s.status === "qualified" ? "default" : "secondary"} className="text-xs">{s.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
