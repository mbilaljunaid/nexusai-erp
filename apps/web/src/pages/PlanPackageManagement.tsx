import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Zap } from "lucide-react"; export default function PlanPackageManagement() { const { data: plans = [], isLoading } = useQuery({ queryKey: ["/api/telecom-plans"], queryFn: () => fetch("/api/telecom-plans").then(r => r.json()).catch(() => []), }); const active = plans.filter((p: any) => p.status === "active").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Zap className="h-8 w-8" /> Plan & Package Management </h1> <p className="text-muted-foreground mt-2">Plans, add-ons, device bundles, usage caps, fair usage policies, seasonal promotions</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Plans</p><p className="text-2xl font-bold">{plans.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active</p><p className="text-2xl font-bold text-green-600">{active}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Price</p><p className="text-2xl font-bold">${plans.reduce((sum: number, p: any) => sum + (parseFloat(p.price) || 0), 0).toFixed(0)}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Price</p><p className="text-2xl font-bold">${plans.length > 0 ? (plans.reduce((sum: number, p: any) => sum + (parseFloat(p.price) || 0), 0) / plans.length).toFixed(0) : 0}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Plans</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : plans.length === 0 ? <p className="text-muted-foreground text-center py-4">No plans</p> : plans.slice(0, 10).map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`plan-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.planId}</p><p className="text-xs text-muted-foreground">{p.dataLimit} GB • {p.voiceLimit} mins</p></div> <Badge variant={p.status === "active" ? "default" : "secondary"} className="text-xs">${p.price}</Badge> </div> ))} </CardContent> </Card> </div> ); }
