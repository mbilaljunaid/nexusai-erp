import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Microscope } from "lucide-react"; export default function LIMSLabManagement() { const { data: tests = [], isLoading } = useQuery({ queryKey: ["/api/pharma-lims"], queryFn: () => fetch("/api/pharma-lims").then(r => r.json()).catch(() => []), }); const passed = tests.filter((t: any) => t.result === "passed").length; const failed = tests.filter((t: any) => t.result === "failed").length; const inProgress = tests.filter((t: any) => t.result === "pending").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Microscope className="h-8 w-8" /> LIMS - Laboratory Information Management </h1> <p className="text-muted-foreground mt-2">Test catalog, instrument calibration, COA, QC charts, method versioning, chain of custody</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Tests</p><p className="text-2xl font-bold">{tests.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Passed</p><p className="text-2xl font-bold text-green-600">{passed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Failed</p><p className="text-2xl font-bold text-red-600">{failed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pass Rate</p><p className="text-2xl font-bold">{tests.length > 0 ? ((passed / tests.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Test Results</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : tests.length === 0 ? <p className="text-muted-foreground text-center py-4">No tests</p> : tests.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`test-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.testId}</p><p className="text-xs text-muted-foreground">Batch: {t.batchId} • Method: {t.method}</p></div> <Badge variant={t.result === "passed" ? "default" : t.result === "failed" ? "destructive" : "secondary"} className="text-xs">{t.result}</Badge> </div> ))} </CardContent> </Card> </div> ); }
