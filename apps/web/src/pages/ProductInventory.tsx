import { useState } from "react"; import { useQuery, useMutation } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Badge } from "@/components/ui/badge"; import { Package, Plus, Trash2 } from "lucide-react"; import { queryClient } from "@/lib/queryClient"; import { useToast } from "@/hooks/use-toast"; export default function ProductInventory() { const { toast } = useToast(); const [newProd, setNewProd] = useState({ sku: "", name: "", category: "", stockQty: "", status: "active" }); const { data: products = [], isLoading } = useQuery({ queryKey: ["/api/retail-products"], queryFn: () => fetch("/api/retail-products").then(r => r.json()).catch(() => []), }); const createMutation = useMutation({ mutationFn: (data: any) => fetch("/api/retail-products", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }).then(r => r.json()), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/retail-products"] }); setNewProd({ sku: "", name: "", category: "", stockQty: "", status: "active" }); toast({ title: "Product added" }); }, }); const deleteMutation = useMutation({ mutationFn: (id: string) => fetch(`/api/retail-products/${id}`, { method: "DELETE" }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ["/api/retail-products"] }); toast({ title: "Product deleted" }); }, }); const inStock = products.filter((p: any) => parseInt(p.stockQty) > 0).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Package className="h-8 w-8" /> Product & Inventory Management </h1> <p className="text-muted-foreground mt-2">SKU, categories, attributes, stock levels, batches, serial tracking, replenishment</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Products</p><p className="text-2xl font-bold">{products.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Stock</p><p className="text-2xl font-bold text-green-600">{inStock}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Out of Stock</p><p className="text-2xl font-bold text-red-600">{products.filter((p: any) => parseInt(p.stockQty) === 0).length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Stock</p><p className="text-2xl font-bold text-blue-600">{products.reduce((sum: number, p: any) => sum + (parseInt(p.stockQty) || 0), 0)}</p></CardContent></Card> </div> <Card data-testid="card-new-product"> <CardHeader><CardTitle className="text-base">Add Product</CardTitle></CardHeader> <CardContent className="space-y-3"> <div className="grid grid-cols-5 gap-2"> <Input placeholder="SKU" value={newProd.sku} onChange={(e) => setNewProd({ ...newProd, sku: e.target.value })} data-testid="input-sku" className="text-sm" /> <Input placeholder="Name" value={newProd.name} onChange={(e) => setNewProd({ ...newProd, name: e.target.value })} data-testid="input-name" className="text-sm" /> <Input placeholder="Category" value={newProd.category} onChange={(e) => setNewProd({ ...newProd, category: e.target.value })} data-testid="input-cat" className="text-sm" /> <Input placeholder="Stock" type="number" value={newProd.stockQty} onChange={(e) => setNewProd({ ...newProd, stockQty: e.target.value })} data-testid="input-stock" className="text-sm" /> <Button disabled={createMutation.isPending || !newProd.sku} size="sm" data-testid="button-add"> <Plus className="w-3 h-3" /> </Button> </div> </CardContent> </Card> <Card> <CardHeader><CardTitle className="text-base">Products</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : products.length === 0 ? <p className="text-muted-foreground text-center py-4">No products</p> : products.map((p: any) => ( <div key={p.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`product-${p.id}`}> <div className="flex-1"><p className="font-semibold">{p.sku}</p><p className="text-xs text-muted-foreground">{p.category}</p></div> <div className="flex gap-2 items-center"> <Badge variant={parseInt(p.stockQty) > 0 ? "default" : "destructive"} className="text-xs">{p.stockQty}</Badge> <Button size="icon" variant="ghost" data-testid={`btn-del-${p.id}`} className="h-7 w-7"> <Trash2 className="w-3 h-3" /> </Button> </div> </div> ))} </CardContent> </Card> </div> ); }
