import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Headphones } from "lucide-react"; export default function CustomerSupportCRM() { const { data: tickets = [], isLoading } = useQuery({ queryKey: ["/api/telecom-support"], queryFn: () => fetch("/api/telecom-support").then(r => r.json()).catch(() => []), }); const resolved = tickets.filter((t: any) => t.status === "resolved").length; const pending = tickets.filter((t: any) => t.status === "pending").length; const avgSLA = tickets.length > 0 ? (tickets.reduce((sum: number, t: any) => sum + (parseFloat(t.slaCompliance) || 0), 0) / tickets.length).toFixed(1) : 0; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Headphones className="h-8 w-8" /> CRM, Customer Support & Loyalty </h1> <p className="text-muted-foreground mt-2">Ticket management, complaint tracking, SLA compliance, loyalty points, campaign targeting, interactions</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Tickets</p><p className="text-2xl font-bold">{tickets.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{pending}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Resolved</p><p className="text-2xl font-bold text-green-600">{resolved}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg SLA %</p><p className="text-2xl font-bold text-blue-600">{avgSLA}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Support Tickets</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : tickets.length === 0 ? <p className="text-muted-foreground text-center py-4">No tickets</p> : tickets.slice(0, 10).map((t: any) => ( <div key={t.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`ticket-${t.id}`}> <div className="flex-1"><p className="font-semibold">{t.ticketId}</p><p className="text-xs text-muted-foreground">{t.issueType} • SLA: {t.slaCompliance}%</p></div> <Badge variant={t.status === "resolved" ? "default" : "secondary"} className="text-xs">{t.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
