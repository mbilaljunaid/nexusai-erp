import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Layers } from "lucide-react"; export default function BOMRouting() { const { data: boms = [], isLoading } = useQuery({ queryKey: ["/api/mfg-bom"], queryFn: () => fetch("/api/mfg-bom").then(r => r.json()).catch(() => []), }); const complete = boms.filter((b: any) => b.status === "complete").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Layers className="h-8 w-8" /> BOM & Routing </h1> <p className="text-muted-foreground mt-2">Bill of Materials, routing sequences, sub-assemblies, costing, scrap calculation</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">BOMs</p><p className="text-2xl font-bold">{boms.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Complete</p><p className="text-2xl font-bold text-green-600">{complete}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Progress</p><p className="text-2xl font-bold text-yellow-600">{boms.filter((b: any) => b.status === "draft").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Complete %</p><p className="text-2xl font-bold">{boms.length > 0 ? ((complete / boms.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">BOMs</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : boms.length === 0 ? <p className="text-muted-foreground text-center py-4">No BOMs</p> : boms.slice(0, 10).map((b: any) => ( <div key={b.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`bom-${b.id}`}> <div className="flex-1"><p className="font-semibold">{b.bomId}</p><p className="text-xs text-muted-foreground">{b.productId}</p></div> <Badge variant={b.status === "complete" ? "default" : "secondary"} className="text-xs">{b.status}</Badge> </div> ))} </CardContent> </Card> </div> ); }
