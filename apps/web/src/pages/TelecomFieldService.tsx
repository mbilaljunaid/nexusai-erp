import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Wrench } from "lucide-react"; export default function TelecomFieldService() { const { data: workorders = [], isLoading } = useQuery({ queryKey: ["/api/telecom-fieldservice"], queryFn: () => fetch("/api/telecom-fieldservice").then(r => r.json()).catch(() => []), }); const completed = workorders.filter((w: any) => w.status === "completed").length; return ( <div className="space-y-6 p-4"> <div><h1 className="text-3xl font-bold flex items-center gap-2"><Wrench className="h-8 w-8" /> Field Service</h1><p className="text-muted-foreground mt-2">Work orders, scheduling, dispatch, SLA</p></div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Work Orders</p><p className="text-2xl font-bold">{workorders.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completed</p><p className="text-2xl font-bold text-green-600">{completed}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Pending</p><p className="text-2xl font-bold text-yellow-600">{workorders.filter((w: any) => w.status === "pending").length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Completion %</p><p className="text-2xl font-bold">{workorders.length > 0 ? ((completed / workorders.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card><CardHeader><CardTitle className="text-base">Work Orders</CardTitle></CardHeader><CardContent className="space-y-2">{isLoading ? <p>Loading...</p> : workorders.length === 0 ? <p className="text-muted-foreground text-center py-4">No work orders</p> : workorders.slice(0, 10).map((w: any) => (<div key={w.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`wo-${w.id}`}><div className="flex-1"><p className="font-semibold">{w.workOrderId}</p><p className="text-xs text-muted-foreground">Tech: {w.technicianId}</p></div><Badge variant={w.status === "completed" ? "default" : "secondary"} className="text-xs">{w.status}</Badge></div>))}</CardContent></Card> </div> ); }
