import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Truck } from "lucide-react"; export default function SalesDistribution() { const { data: orders = [], isLoading } = useQuery({ queryKey: ["/api/cpg-sales"], queryFn: () => fetch("/api/cpg-sales").then(r => r.json()).catch(() => []), }); const totalValue = orders.reduce((sum: number, o: any) => sum + (parseFloat(o.value) || 0), 0); const delivered = orders.filter((o: any) => o.status === "delivered").length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Truck className="h-8 w-8" /> Sales & Distribution </h1> <p className="text-muted-foreground mt-2">Customer master, sales orders, fulfillment, route planning, delivery tracking, trade terms & pricing</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Orders</p><p className="text-2xl font-bold">{orders.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Value</p><p className="text-2xl font-bold text-green-600">${(totalValue / 1000).toFixed(0)}K</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Delivered</p><p className="text-2xl font-bold text-blue-600">{delivered}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Fulfillment %</p><p className="text-2xl font-bold">{orders.length > 0 ? ((delivered / orders.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Sales Orders</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : orders.length === 0 ? <p className="text-muted-foreground text-center py-4">No orders</p> : orders.slice(0, 10).map((o: any) => ( <div key={o.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`order-${o.id}`}> <div className="flex-1"><p className="font-semibold">{o.orderId}</p><p className="text-xs text-muted-foreground">Customer: {o.customerId}</p></div> <Badge variant={o.status === "delivered" ? "default" : "secondary"} className="text-xs">${o.value}</Badge> </div> ))} </CardContent> </Card> </div> ); }
