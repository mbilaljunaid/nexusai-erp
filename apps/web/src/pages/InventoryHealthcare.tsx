import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { Box } from "lucide-react"; export default function InventoryHealthcare() { const { data: inventory = [], isLoading } = useQuery({ queryKey: ["/api/hc-inventory"], queryFn: () => fetch("/api/hc-inventory").then(r => r.json()).catch(() => []), }); const inStock = inventory.filter((i: any) => i.status === "in-stock").length; const expiringSoon = inventory.filter((i: any) => i.daysToExpiry <= 30).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <Box className="h-8 w-8" /> Healthcare Inventory Management </h1> <p className="text-muted-foreground mt-2">Medical supplies, equipment, consumables, batch tracking, expiry management, utilization analysis</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Items</p><p className="text-2xl font-bold">{inventory.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">In Stock</p><p className="text-2xl font-bold text-green-600">{inStock}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Expiring Soon</p><p className="text-2xl font-bold text-orange-600">{expiringSoon}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Stock %</p><p className="text-2xl font-bold">{inventory.length > 0 ? ((inStock / inventory.length) * 100).toFixed(0) : 0}%</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Inventory Items</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : inventory.length === 0 ? <p className="text-muted-foreground text-center py-4">No items</p> : inventory.slice(0, 10).map((i: any) => ( <div key={i.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`inv-${i.id}`}> <div className="flex-1"><p className="font-semibold">{i.itemId}</p><p className="text-xs text-muted-foreground">Expires: {i.daysToExpiry}d</p></div> <Badge variant={i.daysToExpiry > 30 ? "default" : "destructive"} className="text-xs">{i.daysToExpiry}d</Badge> </div> ))} </CardContent> </Card> </div> ); }
