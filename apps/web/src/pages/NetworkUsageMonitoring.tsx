import { useQuery } from "@tanstack/react-query"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge"; import { TrendingUp } from "lucide-react"; export default function NetworkUsageMonitoring() { const { data: usage = [], isLoading } = useQuery({ queryKey: ["/api/telecom-usage"], queryFn: () => fetch("/api/telecom-usage").then(r => r.json()).catch(() => []), }); const totalData = usage.reduce((sum: number, u: any) => sum + (parseFloat(u.dataUsed) || 0), 0); const avgUsage = usage.length > 0 ? (totalData / usage.length).toFixed(2) : 0; const overLimit = usage.filter((u: any) => parseFloat(u.dataUsed) > parseFloat(u.dataLimit)).length; return ( <div className="space-y-6 p-4"> <div> <h1 className="text-3xl font-bold flex items-center gap-2"> <TrendingUp className="h-8 w-8" /> Network Usage & Monitoring </h1> <p className="text-muted-foreground mt-2">Voice, data, SMS tracking, network load forecasting, anomaly detection, usage alerts</p> </div> <div className="grid grid-cols-4 gap-3"> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Active Sessions</p><p className="text-2xl font-bold">{usage.length}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Total Data (GB)</p><p className="text-2xl font-bold text-blue-600">{totalData.toFixed(0)}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Over Limit</p><p className="text-2xl font-bold text-red-600">{overLimit}</p></CardContent></Card> <Card className="p-3"><CardContent className="pt-0"><p className="text-xs text-muted-foreground">Avg Usage (GB)</p><p className="text-2xl font-bold">{avgUsage}</p></CardContent></Card> </div> <Card> <CardHeader><CardTitle className="text-base">Usage Tracking</CardTitle></CardHeader> <CardContent className="space-y-2"> {isLoading ? <p>Loading...</p> : usage.length === 0 ? <p className="text-muted-foreground text-center py-4">No usage</p> : usage.slice(0, 10).map((u: any) => ( <div key={u.id} className="p-2 border rounded text-sm hover-elevate flex items-center justify-between" data-testid={`usage-${u.id}`}> <div className="flex-1"><p className="font-semibold">{u.customerId}</p><p className="text-xs text-muted-foreground">Data: {u.dataUsed}GB / {u.dataLimit}GB</p></div> <Badge variant={parseFloat(u.dataUsed) > parseFloat(u.dataLimit) ? "destructive" : "default"} className="text-xs">{u.dataUsed}GB</Badge> </div> ))} </CardContent> </Card> </div> ); }
